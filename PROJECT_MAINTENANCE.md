# 中国旅游地图项目与数据维护手册

最后更新：2026-08-13

这份文件是项目的长期上下文和操作入口。以后修改景点、图片、攻略或部署流程前，先读本文件；不要只凭聊天记录恢复规则。

## 0. 2026-08-11 当前交接快照

### 已上线且应保持稳定

- 正式站点：`https://xzmap.xzbest.site`，运行在阿里云正式轻量服务器；Nginx 提供静态资源，Node.js `lvyoumap.service` 在本机 `3000` 端口提供健康检查和实时天气。
- 正式 Git 仓库：`D:\github\lvyoumap-universal-server`。贵州、福建、江西和广东四省已经由用户验收并同步；广东核心覆盖为 44/44，四省合计 97/97。
- 北京及后续省份仍只在 beta 迭代。北京当前核心清单草稿为 23 个，多源质量门禁通过，另有 3 个普通单源候选留在观察池；尚未由用户批准，更未同步正式仓库。
- 已上线能力包括：详情大图查看与关闭交互、搜索结果显示省份而省内普通列表不重复显示、备案号轻量布局、实时天气、覆盖层数据生成、中文数据维护总控及三个试点省的质量报告。
- 正式版现在可继续由 GitHub Desktop 推送，并由服务器原有更新机制拉取部署。不要因为全国数据治理而改写这套已稳定的线上部署结构。

### 仅在 beta、尚未发布

- `D:\github\lvyoumap-universal-serverbeta` 是当前全国扩展试验区，工作区存在大量尚未提交的已验证数据文件和新脚本；不得整目录覆盖正式仓库。即使 beta 脚本检查通过，也必须等用户完成数据与页面抽查并明确要求同步，才可写入正式 Git 目录。
- 新增 `scripts/national_core_queue.js` 和新版 `scripts/maintenance_menu.js`，总控范围已改成“推荐批次 / 编号选择单省 / 全国概览”，不再要求手工输入省名。
- 7 个推荐批次为：京津冀鲁（北京、天津、河北、山东）；长三角与华东（上海、江苏、浙江、安徽）；东北（辽宁、吉林、黑龙江）；中部（山西、河南、湖北、湖南）；华南（广东、广西、海南）；西南与高原（重庆、四川、云南、西藏）；西北（陕西、甘肃、青海、宁夏、新疆、内蒙古）。贵州、福建、江西因已完成，不再列入待补批次。
- 全国队列状态保存在 `.runtime/national-core-queue.json`。大陆31个省级地区中已批准3个，剩余28个；这28个均已完成文旅部官方候选和 OTA 候选采集，当前 `officialReady=28`、`otaReady=28`、`errors=0`。
- 剩余28省当前尚未运行小红书核心候选口碑验证，状态为 `xhsReady=0`、`draftReady=0`。界面阶段显示为“核心候选待口碑验证”；这表示只是准备好了静态来源，绝不表示核心清单或景点完整资料已经补全。
- 香港、澳门、台湾标记为 `separate_region_policy`，不得套用大陆5A景区基线，后续单独建模。

### 下一步唯一主线

1. 在 beta 双击 `数据维护总控.bat`，选择“开始/继续增量补全 → 推荐批次”，按批次逐步运行，不一次请求全国口碑平台。
2. 每批依次完成：复用官方与 OTA 候选 → 小红书点点核心候选 → 高德本地底库交叉匹配 → 生成多源核心草稿 → 人工复核并输入 `Y` 批准。
3. 批准核心清单只代表建立质量基线，不代表景点已补全。随后必须生成缺失/别名报告和修复档案，补齐基本信息、旅行指南、点点懒人攻略、授权图片与事实来源。
4. 每省通过严格报告和搜索、列表、详情、大图、天气的跨端抽查后，才算完成；再按窄文件范围同步到正式仓库，由用户提交发布。
5. 广东已作为第四个样板走通整条补全闭环并同步正式仓库；当前在 beta 推进“京津冀鲁”，北京草稿等待用户审批与后续完整资料补全。

### 不属于旅游项目的插曲

- `D:\github\xzgp` 股票查询项目已独立部署为服务器 `xzgp.service`，域名为 `https://xzgp2.xzbest.site`，与旅游项目的数据、代码、端口和更新机制无关。
- XZGP 使用本机端口 `5200`、独立 Nginx 虚拟主机和独立证书。不要把它的 Flask、Gunicorn或宝塔部署讨论混入旅游地图的 Node.js 通用服务器方案。

## 1. 当前产品和仓库边界

- `D:\github\lvyoumap-universal-serverbeta`：本地试验版，所有数据迭代先在这里完成和验收。
- `D:\github\lvyoumap-universal-server`：正式 Git 仓库。只有 beta 验收通过后，才做窄范围同步并由用户决定提交、推送。
- `D:\github\lvyoumap`、`D:\github\lvyoumap-edgeone`：历史参考，只读，不再作为新数据的直接维护源。
- 正式站点为 `https://xzmap.xzbest.site`，通用服务器版不绑定阿里云、腾讯云、宝塔或特定 CDN。

禁止把整个 beta 文件夹覆盖到正式仓库。正确顺序是：beta 修改 → 本地验证 → 查看差异 → 仅同步确认过的文件 → 用户用 GitHub Desktop 提交和推送。

强制发布边界：用户在 beta 输入 `Y` 只代表批准本省核心清单，不代表允许同步正式 Git；完成完整资料补全、严格报告和页面抽查后，必须再次取得用户明确的“同步正式目录”指令。不得因修复总控或运行测试而把脚本、`.runtime` 缓存、草稿或报告提前复制到正式仓库。

## 2. 运行架构

- 页面、地图、景点 JSON 和本地图片由 `dist` 静态提供。
- Node.js 只承载 `/api/health` 和 `/api/weather`，因此实时天气仍然存在；它不是纯静态网站。
- `scripts/build.js` 在部署时生成 `dist`，平时不需要手工压缩上传。
- 云服务器轮询 GitHub 时只在提交发生变化后拉取、构建、健康检查和原子切换；未更新时只是一次轻量提交号检查。
- `content/db.json` 是历史全国基础库，不进入公开产物；不要为了补一个景点直接批量重写它。

## 3. 数据分层与优先级

构建时按以下顺序合并，后层覆盖前层：

1. `content/db.json`：历史全国基础数据。
2. `content/manual-attractions*.json`：经过人工核验的新景点；贵州、福建、江西分别维护在对应的 `manual-attractions.<province>-core.json`。
3. `content/attraction-overrides.json`：给已有景点补字段，不复制整条记录。目前用于遵义会议会址。
4. `content/lazy-guide-overrides.json`：小红书点点采集结果，只允许覆盖 `lazy_ai_text` 和 `lazy_ai_source`。
5. `content/image-overrides.json`：已核验的图片替换。

生成文件是 `data/provinces-index.json`、`data/provinces/*.json`、`data/search-index.json`。这些文件只能由生成脚本更新，不手改。

## 4. 一个核心景点的完成标准

新增景点必须一次完成三块，任何一块缺失都不能算完成：

- 基本信息：名称、城市、介绍、等级、地址、开放说明、票务说明、提示、至少两个来源、图片来源和许可证。
- 旅行指南：四季穿衣、外部到达、景区内部交通、住宿区域、至少三项当地食物、老人和儿童建议。
- 懒人攻略：前端优先展示完整的 `lazy_ai_text` 文章；同时保留至少一条来源明确、真实可执行的结构化 `lazy_routes` 作为兜底。第二条仅在确有不同玩法时补充，不设置固定节点数。

文章式懒人攻略至少应包含：明确游览顺序、老人或儿童适用性、省力交通或少走路办法、时长或折返点、风险与当日公告提醒。只有“路线一/路线二”两张模板卡但没有文章，不算完成。

票价、开放时间、接驳和演出属于易变信息。不得写成永久事实，必须标注“以官方当日公告为准”，并更新核验日期。

### 4.1 质量门禁分级（2026-08-13）

总控采用三级处理，不再把所有资料缺口都当成程序失败：

1. **关键阻断**：景点身份或属地冲突、疑似重复新增、没有任何可追溯基本来源、基本介绍或地址为空、点点攻略缺失、没有任何真实可执行游览方案、攻略含固定价格/固定时刻/错景点内容等风险。遇到这些问题必须停止，不能进入隔离预览。游览方案至少一条即可，节点按景点实际组织，不因路线数或节点数不足机械阻断。
2. **真实图片门禁**：图片依次复用已核验条目主图、官方页面、高德同实体 POI、携程同实体景点页、维基/Commons 和公开图片搜索结果。占位图永远不算完成，也不得进入隔离预览或正式数据。若单个景点仍未取得实体匹配且清晰度达标的真实图片，只保留该景点为可续跑断点，下次单独继续搜图，不重复已经完成的景点、攻略和路线。
3. **后续增强**：更丰富的图片、更多来源、文案润色等体验提升项，记录在质量报告中，不应让整省流程反复中断。

图片补全以家庭自用体验优先：授权字段记录来源，但不作为放弃真实图片的理由；实体与属地匹配、排除地图/导览/海报、清晰度达标是硬门槛。设施型及复合名称会生成实体主干和组成景点别名，例如“天津之眼摩天轮”检索“天津之眼”，“小三峡－小小三峡旅游区”同时检索两个组成名称。所有网络图片下载为本地资源，并保留来源页、提供方和原始尺寸，便于追溯和后续替换。

“隔离预览就绪”不等于已经写入 beta。最终仍需用户查看页面并第二次输入 `Y`；未批准时补全包、警告和预览均保留，正式数据不变。

核心清单研究阶段已经收集的文旅部、景区官方、携程、高德和小红书来源，资料补全阶段必须复用，不能因为脚本层之间没有传递而误报“来源不足”。高德详情只在唯一同实体且 POI 类型为景点时用于补地址或评分。

## 5. 小红书点点攻略机制

历史机制已在通用服务器 beta 中恢复，日常入口统一为 `数据维护总控.bat`。主菜单只保留四个结果入口：一键数据体检、开始/继续增量补全、任务中心、生成发布数据并完整验收。登录、进度窗口和安全停止归入任务中心，范围选择归入体检与补全，不把底层脚本动作平铺成十几个菜单项。任务中心 `[2] 打开当前任务` 是统一入口：任务运行中打开持续进度窗口；隔离预览就绪后直接打开浏览器预览。预览服务意外停止或前端构建发生变化时，总控会尝试自动重建，不需要手工复制 `127.0.0.1` 地址。

推荐操作顺序：

1. 先运行“一键数据体检”，选择全国或指定省份；总控生成 `reports/maintenance-tasks.json`，区分核心阻断项、可自动补全项和建议优化项。
2. 选择“开始/继续增量补全”；总控会再次体检并只处理缺失或待复核的点点文章。
3. 采集在后台运行，总控可以关闭；再次打开仍能查看进度或安全停止。
4. 完成后选择“生成发布数据并完整验收”；成功结果逐条保存，失败和未完成项不写入，下次运行自动续补。

若补全包已经通过质量门禁并标记为 `reviewed`，再次选择“开始/继续”只会恢复或重建隔离预览，不会重新打开小红书，也不会重复进行整省联网采集。已有景点通过 `preferredId` 自动转换为覆盖增强，不得作为新景点重复写入。

当体检发现真实缺失核心景点时，总控不会直接启动点点或自动新增，而是先运行 `scripts/core_repair_pipeline.js` 生成 `.runtime/core-repairs.<province>.json`。档案会区分真实缺失、别名/重复风险和已有记录字段不完整，并保存官方结构化证据。只有 `content/core-repair-packages.<province>.json` 标记为 `reviewed`，且景点身份、基本信息、旅行指南、结构化路线、至少一个可追溯事实来源和点点文章通过关键质量闸门后，总控才会生成隔离预览。第二来源或授权实景图暂缺会作为警告展示，而不是伪造内容或提前写入。用户完成预览并再次输入 `Y` 后才写入 beta；已应用的包改为 `applied`，不会重复采集或重复新增。

最终报告与隔离预览使用同一门禁：人工预览已确认、具备至少一个权威可追溯来源的记录允许带“单来源待增强”警告通过，不得在最终生成阶段重新升级为整省阻断。官方短泛称与已绑定完整景点名若为包含关系且属地兼容，总控自动合并并保留完整名称，避免同一景点重复计数。

省份写入采用事务保护：总控在应用补全包前暂存该省核心清单、人工数据层、覆盖层和补全包；生成、构建、页面校验或省级严格报告任一步失败，都会自动恢复批准前文件并重建派生数据。失败资料和隔离预览继续保留供修复续跑，不允许 beta 留下半应用状态。

### 上海断点回归（2026-08-13）

- 总控完整流程已跑到隔离预览：19/19，成功 19，失败 0。
- 补全包结构为新增 18 条、增强已有 1 条；`上海野生动物园` 唯一匹配现有 `amap_B00152F15F`，已自动转为覆盖增强，没有重复新增。
- 旧版本曾让 8 条图片缺口以占位图进入隔离预览；该策略已经废止。当前总控会自动续跑真实图片搜索，占位图不再被认定为完整资料，也不能进入新隔离预览。
- 修复了上海地名“海”导致野生动物园被误判为滨海类型的问题，并以官方来源正确标记为 5A 景区。
- `reviewed` 状态再次续跑会直接恢复隔离预览，验证不会重新采集小红书或重复联网。

也可以使用以下底层命令：

```powershell
npm run xhs:login
npm run xhs:list:guizhou
npm run xhs:guizhou
npm run xhs:stats
npm run xhs:global
```

固定提示词沿用旧版：

```text
景点名 长辈小孩省力路线，不要无关内容（如餐饮推荐）
```

采集规则：

- 使用 `.runtime/xhs-profile` 保存本机登录状态，该目录不提交 Git。
- 默认只处理没有点点来源或标记“待点点复核”的景点；已有正常点点文章全部跳过。
- 回答长度、路线信号、老幼适用性、安全提醒和文章结构都通过后才写入。
- 点点回答写入前会清除固定时刻和对话式追问；补全包质量闸门会再次检查固定时刻、固定价格、对话式结尾，以及该景点配置的错景点禁用词。命中任何内容风险时必须重新采集或人工复核，不能只凭“字段齐全”批准。
- 独立演出类景点使用专用提示词和验收信号，禁止把所属度假区的其他主题园路线混入；例如广州长隆国际大马戏不能混入动物世界、欢乐世界和水上乐园攻略，也不建议以提前离场作为避拥挤方案。
- 登录失效、回答截断或质量不足时不写入；进度和失败样本在 `.runtime` 中保留。
- 自动化浏览器使用独立的 `.runtime/xhs-profile`，与日常 Chrome 登录互不继承；通常只需首次扫码，平台要求重新验证时再登录。
- 采集前先访问首页确认会话，景点之间默认至少间隔 8 秒；“访问受限”和“未登录”分别处理。触发限制时安全暂停，不反复刷新或反复扫码。
- 每条成功结果立即原子写入 `content/lazy-guide-overrides.json`，首次写入前创建本地备份。
- 采集器绝不修改 `content/db.json`，也不覆盖景点的基本信息、图片或旅行指南。
- 不绕过二维码、验证码、平台风控或账号验证。
- 总控不提供强制覆盖入口，避免误刷新已经合格的攻略；开发者底层的 `--force` 只用于明确审计后的人工修复。
- `.runtime/xhs-lazy-progress.json` 保存跨窗口进度，`.runtime/xhs-lazy-process.log` 保存后台日志，`.runtime/xhs-lazy-stop.flag` 用于在当前景点完成后安全停止。

`lazy_ai_source.source` 的含义：

- `xiaohongshu-dian-dian-ai-chat`：已由点点实际生成并通过机器完整性检查。
- `multi-source-editorial-pending-xhs-verification`：根据官方和公开旅游资料整理的可用稿，仍需点点复核替换；不能冒充小红书结果。

## 6. 贵州试点状态

贵州核心清单维护在 `content/core-attractions.guizhou.json`，当前共 15 个：黄果树、梵净山、荔波小七孔、西江千户苗寨、织金洞、中国天眼、青岩古镇、万峰林、肇兴侗寨、遵义会议会址、赤水丹霞、百里杜鹃、龙宫、镇远古城、马岭河峡谷。

2026-08-10 已完成：

- 核心覆盖由 10/15 提升到 15/15。
- 西江千户苗寨补齐基本信息、旅行指南、结构化路线和文章式懒人攻略。
- 新增中国天眼、万峰林、肇兴侗寨、镇远古城、马岭河峡谷完整记录和可授权图片。
- 遵义会议会址通过覆盖层补齐基本信息与旅行指南。
- 新增严格质量报告，核心景点必须同时通过基本信息、旅行指南、懒人攻略和来源检查。

贵州全库里仍可能存在车站、普通广场等低价值 POI。它们不应为了追求“所有记录100%”而自动生成攻略；核心清单和真实游客价值优先。

### 福建试点状态

福建核心清单维护在 `content/core-attractions.fujian.json`，共 17 个检索单元：14 个官方核心游览单元，以及平潭国际旅游岛、泉州开元寺、霞浦滩涂 3 个长期口碑目的地。

2026-08-10 已完成：

- 核心覆盖达到 17/17，0 个别名待确认、0 个完全缺失。
- 新增霞浦滩涂完整记录，按北岐、东壁、小皓、花竹等分散摄影点建模，不伪装成单一售票景区。
- 霞浦滩涂基本信息、旅行指南、结构化路线、图片许可和事实来源齐全；懒人攻略已由小红书点点复核替换。
- 福建全省 209 条记录中排除 2 条低价值设施类记录，其余 207 条均已有点点文章，待自动补全为 0。
- 16 个历史核心景点仍有图片授权或基础来源追溯的“建议优化”项；这不阻断当前使用，也不得为了消除提示而伪造许可或批量替换正常图片。

### 江西试点状态

江西核心清单维护在 `content/core-attractions.jiangxi.json`，共 21 个。2026-08-10 已完成首轮通用“核心缺失补全包”验证：

- 核心覆盖由 19/21 提升到 21/21，0 个别名待确认、0 个真实缺失，21/21 通过当前严格质量报告。
- 新增龟峰景区、井冈山风景旅游区，均包含文旅部官方身份与地址简介、完整旅行指南、两条来源明确的结构化路线、点点文章和 Wikimedia Commons 开放许可图片。
- 望仙谷保留原景点 ID，通过 `attraction-overrides.json` 补齐旅行指南、事实来源和开放许可图片，没有复制成第二条景点。
- 江西补全包保留在 `content/core-repair-packages.jiangxi.json` 且状态为 `applied`，可作为后续省份的字段模板；实际写入内容分别落在小型手工层和覆盖层。
- 补全脚本先做别名、城市、现有 ID 和疑似包含关系检查；发现疑似重复时只进入复核，不允许自动新增。

### 广东总控回归状态

广东核心清单维护在 `content/core-attractions.guangdong.json`，共 44 个。2026-08-12 已由总控链路完整走通：

- 核心覆盖由 32/44 提升到 44/44，0 个别名待确认、0 个真实缺失，44/44 通过严格质量报告。
- 新增 12 个完整核心记录，写入 `content/manual-attractions.guangdong-core.json`；原基础库和其他省份均未覆盖。
- 12 个记录均包含基本信息、旅行指南、至少两条结构化路线、点点文章、至少两个事实来源和可追溯图片；巴伐利亚庄园的错误导览图已替换为景区实景，东部华侨城已替换为与授权信息一致的开放许可原图。
- 大马戏的点点回答曾错误混入动物世界路线，由新增的景点专属内容风险闸门拦截并重采；该规则已做成全国通用能力。
- 补全包保留在 `content/core-repair-packages.guangdong.json` 且状态为 `applied`，所有正式写入前均有 `.runtime/backups` 时间戳备份。
- 已执行 `npm run check`、`npm run build`、`node scripts/verify-build.js`，并从总控菜单实际选择“单个省份 → 广东”复测，界面正确显示“44 个核心景点，44 个资料就绪，0 待核验，0 真实缺失”。

## 7. 验证和回撤

每次数据改动后执行：

```powershell
npm run verify
```

验收包括 JavaScript 语法、静态数据生成、部署产物检查，以及所有已建立省级核心清单的严格质量报告。当前严格覆盖贵州、福建和江西，报告分别写入对应的 `reports/core-attractions-<province-key>.json`。

上线前还要人工抽查：

1. 搜索新增景点能显示正确省份和城市。
2. 基本信息、旅行指南、懒人攻略三个标签都有真实内容。
3. 图片主体与景点一致，点击大图后清晰。
4. 手机、平板、PC 的列表与详情不溢出。
5. 实时天气接口仍可访问。

回撤优先删除或恢复小型覆盖层文件，不回滚整个基础库：

- 攻略问题：恢复 `content/lazy-guide-overrides.json`，本地备份在 `.runtime/backups`。
- 已有景点字段问题：恢复 `content/attraction-overrides.json`。
- 新增景点问题：只撤回对应 `manual-attractions.*.json` 记录和图片。
- 图片问题：只撤回对应图片覆盖项和图片文件。

## 8. 后续推广到全国的顺序

先验收贵州，再逐省推进：

1. 建省级核心清单，官方文旅或A级目录确认身份，OTA和大众点评只辅助判断游客认知与口碑。
2. 生成缺失报告，不以总记录数代替核心覆盖率。
3. 逐个补齐完整三块数据和图片授权。
4. 仅对新增或待复核景点运行点点采集，不刷新已有正常文章。
5. 运行严格报告和跨省回归，再进入正式仓库。

“全国增量”是长期自动补缺能力，不代表应首次就无人值守地改全国 5000 多条记录。建立核心清单和数据质量标准仍应按省推进；总控会跳过已有合格结果，并且所有写入只进入可回撤覆盖层。

总控已经补上“无省级清单时自动建草稿”的前置流程。选择指定省份后，系统依次采集文旅部、携程和小红书候选，与本地高德 5330 条底库交叉匹配；首轮单源重要候选还会自动进入城市级携程分页、高德唯一 POI 和官方身份二次补证，再重建最终草稿。窗口只展示最终纳入与“二次补证后仍待人工”名单；只有人工输入 `Y` 才会写入 `content/core-attractions.<province-key>.json`。全国模式只体检已经批准的省份，并明确显示已纳管省份数，避免把尚未建清单的省误报为完整。

### 全国核心景点诊断框架

省级核心清单统一采用 `content/core-attractions.<province-key>.json`，当前已有贵州和福建。清单把入选依据分成两类：

- 官方核心：文化和旅游部国家5A级景区作为硬基线；国家级旅游度假区同样属于文旅部结构化官方身份，可直接进入核心清单，并用官方地址和坐标校验城市。后续可继续接入世界遗产、国家公园或省级规划中的代表性目的地。
- 口碑核心：携程等 OTA 的稳定高关注目的地，以及大众点评、小红书中长期存在的高频真实游览目的地。

大众点评和小红书的价值主要是发现游客认知、近期体验问题和攻略需求；不得用动态评分、帖子数量或单篇内容替代官方事实。

当前自动建清单的数据职责如下：

- 文旅部大众旅游服务省级页：读取国家5A级景区与国家级旅游度假区，页面摘要数量必须和解析结果一致，否则拒绝写入；入口为 `https://lyfw.mct.gov.cn/site/special/home`。
- 文旅部省级页的每条官方记录同时保留名称、地址、简介、经纬度和官方图片相对路径；解析只接受页面中直接出现的字符串字段，不执行页面脚本，也不把动态天气写入内容库。
- 高德本地快照：5330 条景点候选池，用于覆盖发现、别名匹配和省内相对排序，不单独决定核心身份。
- 携程省级热门景点页：提供稳定的跨区域游客认知信号，不固化动态评分、票价或点评量。
- 小红书点点：用统一提示词发现18至25个长期高频口碑目的地。正常回答10至15秒；开始输出后停滞3秒会尝试续写或立即重试，最多3轮，合并后不足18个只保存诊断草稿，绝不写入正式候选。

非官方景点至少命中两个独立来源维度才进入最终草稿。首轮只有一个来源但可能重要的候选不会直接丢弃，而是先进入自动二次补证：要求城市兼容、名称高置信，并在城市级携程景点分页、高德唯一 POI 或已核验官网中找到独立证据；仍无法确认的项目进入观察池，省榜高位项额外标为优先观察，但不再阻断整省其他已确认景点。真正的阻断项仅包括来源采集链路不完整、官方硬基线异常，以及已经纳入清单的实体错配、跨城市绑定或证据不足。二次证据文件仍必须覆盖本轮全部首轮单源候选，旧证据或残缺证据会触发同批自动重建。此处的“来源维度”用于选点，不替代详情页中开放时间、票价、地址等事实字段的独立核验。

大众点评可作为口碑补充证据，但目前没有适合无人值守全国任务的稳定公开检索接口。因此总控不会伪装成已接入，也不会把搜索摘要或不可复现页面计分；只有未来获取到可公开访问、能核验景点名称与城市的具体页面时，才写入 `dianping_public_listing`。大众点评不可用不会阻塞文旅部、携程、高德和小红书组成的主闭环。

自动补证仍无法覆盖、但已经通过人工核验的长期知名景点，可把景点官网、政府文旅页或可靠口碑平台的具体页面写入 `content/core-evidence-overrides.json`。每条必须包含省份、景点名、城市、HTTPS 来源页和核验日期；动态评分、搜索结果摘要和无法复现的页面不得写入。该文件只补充“核心身份”证据，不会直接新增展示数据或绕过完整资料质量门禁。

运行以下命令只会读取数据并生成报告，不会新增或覆盖景点：

```powershell
npm run report:core
npm run report:core:fujian
node scripts/report_core_attractions.js --province=贵州
```

每省报告写入 `reports/core-attractions-<province-key>.json`，全国汇总写入 `reports/core-attractions-national.json`。报告区分：准确存在、疑似别名/重复需复核、完全缺失，并给出基本信息、旅行指南、懒人攻略、图片来源与事实来源问题。“一键数据体检”会进一步生成 `reports/maintenance-tasks.json`，总控后续只围绕这张任务清单续跑。

质量等级含义：

- `stable`：主要字段完整且没有来源、图片或结构问题。
- `improve`：内容可用，但图片授权、事实来源等仍可提升。
- `incomplete`：基本信息、指南或攻略存在明显缺口。
- `priority`：严重缺失，应优先治理。

福建第二试点纳入 17 个核心候选：福建现有12家13处5A体系按实际检索需求拆成14个游览单元（福建土楼按永定/南靖、白水洋—鸳鸯溪按两个入口分别检查），再加入平潭国际旅游岛、泉州开元寺、霞浦滩涂3个主流口碑目的地。当前已完成 17/17，可作为贵州之后的第二份省级样板。

## 9. 常用命令

```powershell
npm install
npm run check
npm run report:core
npm run report:tasks
npm run quality:guizhou
npm run verify
npm start
```

Windows 用户也可以双击：

- `start_dev.bat`：用户已有的本地开发入口，保持不变。
- `数据维护总控.bat`：全国中文总控，主菜单固定为体检、增量补全、任务中心、生成验收四项。

### 全国批次与断点队列

总控的范围选择不再要求手工输入省名，统一使用两级编号菜单：先选“推荐批次 / 单个省份 / 全国概览”，再选批次或省份编号。推荐批次固定为京津冀鲁、长三角与华东、东北、中部、华南、西南与高原、西北七组，每组 3 至 6 个省级地区，既降低连续访问口碑平台的风险，也便于失败后从同一批次续跑。

菜单阶段名称统一采用“对象 + 当前阶段 + 下一步”，依次为“核心候选来源待采集、核心候选来源采集中、核心候选待口碑验证、核心候选已完成口碑验证，待生成清单草稿、核心候选待二次补证、核心清单待人工批准、核心清单已建立，完整资料待补全、完整资料已通过验收、当前阶段失败，可断点续跑”。只有核心景点的基本信息、旅行指南、懒人攻略、图片等完整资料通过质量检查后，才显示“完整资料已通过验收”。全国队列保存在 `.runtime/national-core-queue.json`；文旅部和 OTA 静态候选、点点候选、二次证据及未批准草稿也只保存在 `.runtime`。重新选择同一批次时会跳过已成功环节，不会重新覆盖已完成省份。

核心清单审批前执行结构化质量门禁：携程候选先过滤演唱会、音乐节、带届次展会等临时活动；名称匹配必须同时满足城市兼容与高置信名称关系，不得仅凭“度假区、风景区、博物馆、公园”等通用后缀合并；同名多城市或本地多记录歧义不得自动绑定。审批摘要逐项展示文旅部、携程省级/城市分页、小红书和高德快照证据、现有记录绑定状态、被过滤的临时活动和观察池。大众点评在真正接入前必须明确显示“未接入且不计分”，不得笼统算作已完成口碑交叉验证。任何入选项存在跨城市绑定、临时活动或非官方候选证据不足时，质量门禁阻止批准；二次证据未覆盖当前首轮候选时自动重建；仅观察池存在未确认候选时允许本省继续。

批次流程固定为：采集静态来源 → 逐省采集点点候选 → 生成首轮草稿 → 自动二次补证 → 重建最终草稿 → 显示复核摘要 → 人工输入 `Y` 批准。批准核心清单仍不等于新增景点；缺失景点继续进入修复档案和补全包流程，未通过重复识别与完整字段质量门禁前不得写入展示数据。香港、澳门、台湾不套用大陆 5A 基线，后续单独建立地区规则。

批准核心清单后，总控必须先判断该省处于哪一个“完整资料”阶段，不能假设每省都已经存在人工核验种子：

1. `researching`：自动从补全档案、文旅部记录、携程/小红书候选和二次证据建立资料研究任务；允许先采集点点攻略，但基本信息、路线、事实来源和图片仍为空的字段不得用模板补齐。
2. `collecting`：已存在经过核验的 `content/core-repair-seeds.<province-key>.json`，基本信息、至少一条真实可执行游览方案、至少两个事实来源和可授权图片已经备齐，只差点点攻略。
3. `reviewed`：所有字段和点点攻略均已通过补全包质量门禁，等待本机确认写入。
4. `applied`：已经写入可回撤内容层，并重新绑定核心清单 ID。

如果某省没有核验种子，`prepare_core_repair_package.js`应生成 `reports/core-research-<province-key>.json` 和 `.runtime/core-repair-research.<province-key>.json` 研究工作区，不得抛出 Node 堆栈，也不得把广东人工样板当成全国默认前置条件。研究中草稿不得进入 `content`，防止 Git 提交时误带入正式内容层。旧版项目的高德 5330 条数据只是本地历史快照；原始总控引用的全量更新脚本已经缺失，因此不能把历史快照描述为实时补全 API。

日常使用不要求每次机械地先按 `[1]` 再按 `[2]`：`[2] 开始 / 继续增量补全`内部会自动执行必要的只读体检和状态恢复。`[1]`适合只想看报告、首次建立/批准核心清单，或怀疑数据异常时单独运行；同一个省已有批准清单和补全状态时，直接选择`[2]`即可续跑。

### 当前一键补全闭环

总控 `[2]` 对任意已批准核心清单的省份统一执行以下通用流程，不依赖贵州、广东或北京专用模板：

1. 点点生成完整文章式懒人攻略；
2. 点点生成至少一条真实可执行游览方案，以及交通、住宿、体力和家庭出行建议；确有明显不同玩法时再补第二条；
3. 文旅部用于官方身份和等级，携程等主流 OTA 用于地址、介绍和公开评分，Wikidata 用于实体交叉核验，Wikimedia Commons 用于许可明确的原始高清图；
4. 名称、别名、城市、母子景区关系、来源数量、图片真实尺寸和完整字段统一过质量门禁；
5. 只生成隔离预览，不修改 `content/db.json`；用户验收后再次选择同省并输入 `Y`，才写入 beta 内容层。

每个成功景点都会写入 `.runtime` 原子断点。再次运行默认复用完整断点，不重复请求外部平台；只有显式使用 `--refresh` 才强制刷新基本资料。失败项标记为“可断点续跑”，不清空已成功项目，也不拿模板或虚构评分凑数。没有可核验公开评分时前端显示“暂无公开评分”。人工 `content/core-repair-evidence.<province-key>.json` 只作为疑难实体的可选人工覆盖，并非全国流程必需项。

游览方案采用语义完整性判断：至少存在一条有标题、真实游览顺序或重点、体力等级、步行说明和实用提示的方案即可。山岳景区通常会有更多线路节点，博物馆可按楼层/展区组织，古镇可按街区组织，乐园可按项目顺序组织；不要求固定节点数，也不强制虚构第二条路线。交通、入口、内部移动、住宿区域、长辈建议和儿童建议仍需同时完整。点点回答末尾的“活动”等平台杂字允许清理；新一轮短错误页不得覆盖旧的较完整答案断点。2026-08-14 天津自然博物馆回归已从原 13/14 恢复为 14/14，三层至一层的单条三层级方案通过，未再次联网采集。

### 全国统一容错与自恢复

总控不得按省份增加临时分支。所有省级任务统一解释子脚本状态：退出码 `0` 为完成；退出码 `2` 为“成功断点已保存、尚有少量待续跑”，由协调器短等待后自动重试；退出码 `1` 只有在程序错误、文件结构错误或质量硬冲突时才视为不可自动恢复。小红书登录失效、访问限制和用户安全停止单独归为“需要用户动作”，不得被盲目重试。

自动恢复遵循以下边界：点点攻略、结构化路线、事实资料和二次证据最多进行有限短重试；再次运行只处理失败项。单个省份普通失败不得终止全国批次，队列记录该省断点后继续下一个省；只有全局登录/访问限制才在安全点暂停批次。普通来源暂缺、评分缺失、许可图片暂缺和单源观察候选不属于程序崩溃；它们进入警告或观察池。跨省市绑定、同名异地混淆、临时活动误入、核心身份冲突、基本介绍/地址完全无可靠依据、路线缺失等关键问题仍必须阻断，不能为了“跑通”而降级成假数据。

隔离预览也必须具备通用自愈能力，而不是按省打补丁：

- 补全包内部出现近似名称时，只有同城市且共享同一个携程、Trip.com 或高德实体页，才自动合并；信息更完整、名称更具体的一条作为主记录，路线和来源做无损合并。
- 新增项与底库已有景点规范名称完全一致时，自动转换为该旧记录的“增强覆盖”，避免重复展示；近似名称只有共享强实体证据时才允许自动转换。
- 同名异地、母景区与子景点、仅文字相似但没有实体证据的记录不得自动合并，仍作为关键身份冲突阻断。
- 预览先在临时目录完整构建和校验，成功后才替换旧预览；失败时旧预览继续可用。系统统一停止失效的旧省份预览，并在默认端口被占用时尝试有限备用端口。
- 预览恢复成功后必须把任务中心状态改回 `preview_ready` 和 `100%`，不能保留已经恢复的旧错误状态。自动修复前备份原补全包，动作写入 `selfHealActions`，便于追溯与回撤。

容错规则集中在 `scripts/pipeline_resilience.js`，由省级完整补全、核心草稿流水线和全国队列共同调用；新增来源或脚本时必须接入同一分类器并补充 `scripts/test_pipeline_resilience.js`，禁止在菜单或单个省份中复制一套新的退出码判断。

核心清单建立统一调用 `scripts/core_draft_pipeline.js`，菜单单省模式与全国队列不得再各自拼接首轮草稿、二次补证和最终草稿命令。流水线每次强制使用同一批候选重建三份产物，并检查：二次证据覆盖当前全部待补证候选、观察池没有泄漏进最终清单、最终入选项没有证据门禁问题。发现旧断点不一致时自动完整重建一次；仍失败才保留断点并给出简短原因。高德实时补证必须先通过景点 POI 类型门槛：景区、公园、游乐场、水上活动中心，以及作为游客目的地候选的博物馆、图书馆、美术馆、科技馆等文化场馆可以参与唯一同实体核验；公交站、停车场、酒店、餐饮、售票处等不得作为景点身份或评分证据。2026-08-14 天津回归已覆盖“天津博物馆/国家海洋博物馆不得误并”“天津古文化街官方全名不得被过度剥离”“玛雅海滩水公园和滨海图书馆可由高德精确实体补证”，天津草稿 24 项、0 阻断、质量门禁通过。

北京已作为无人工证据前置的通用回归样板：11 个真实缺失核心景点均具备至少两个独立来源、两条路线、完整旅行指南、点点懒人攻略和通过真实像素门禁的许可图片；生成预览前后 `content/db.json` 保持不变。别名回归覆盖水立方/国家游泳中心、八达岭—慕田峪、古北水镇和北京大运河等组合名称，并明确拒绝异地同类景点误合并。

总控面向人工阅读只显示三级信息：当前步骤、数量结论、需要处理的景点。技术文件路径、完整来源数组和过滤明细默认写入 `.runtime` 草稿，不在成功流程中连续输出；失败时才回显错误详情。最终核心名单按城市分组，每行最多三个景点，避免 Windows 终端自动换行后失去结构。

### 后续“做减法/纠错”阶段（核心加法完成后执行）

先完成全国核心景点的发现与补全，再单独进行存量数据治理，避免加法和清理同时发生导致误删。纠错阶段至少检查：省市归属错误、同名异地误合并、别名重复、母景区与子景点混淆、已闭园或临时活动残留、图片与景点错配。城市归属必须以结构化地址、坐标反查和至少一个可靠事实来源共同确认，不得只凭名称猜测或批量迁移。

广东长隆体系作为回归样例：`广州长隆旅游度假区` 位于广州番禺，`长隆野生动物世界` 也属于广州；`长隆海洋王国` 位于珠海横琴。`珠海横琴长隆国际海洋度假区` 是母度假区名称，不能仅因包含“长隆”就与广州长隆合并，也不能未经层级判断直接与子景点“长隆海洋王国”去重。当前广东底库中上述三条城市字段正常；后续全国纠错仍需用同一规则扫描其他景点。

## 10. 维护红线

- 不直接改正式服务器或正式仓库来试错。
- `lvyoumap-universal-serverbeta` 是唯一开发、采集、修复和验收目录；只有用户明确说“可以同步”或“复制到正式 Git”后，才允许修改 `lvyoumap-universal-server`。通用修复、测试文件和文档更新也不得绕过此确认门。
- 同步正式 Git 时禁止复制 `.env`、密钥、`.runtime`、`node_modules`、`dist`、`release`、日志、临时文件和备份；同步后也不得自动 commit、push 或部署。
- 不整库覆盖，不把生成文件当内容源。
- 不用未经授权或来源不明的图片替换现有素材。
- 不把 OTA 评分、点评量、票价视为永久事实。

### 评分补全层

总控补全景点资料时会独立处理评分，不再要求人工逐条补星级：

1. 已从携程等具体景点详情页取得、且绑定同一实体 URL 的评分优先保留。
2. 缺少 OTA 评分时，在本省 5330 条高德历史快照中进行“省份/城市 + 规范名称 + 单一 POI”匹配；只有唯一同实体才复用高德评分，并记录 POI ID、来源页和核验日期。
3. 组合景区不会继承其中某一个组成景点的评分；同名多记录、跨城市或身份有歧义时保持“暂无公开评分”。
4. “官方认证”是旧数据占位文案，不是点评数量。前端对高德记录只显示“高德地图”，只有取得真实点评数量时才显示“××条点评”。
5. 没有评分不会阻断基本资料、路线、图片和攻略补全，禁止用 5.0 或平均分兜底。
6. 核心清单若已经绑定 `preferredId`，资料工作区会携带该 POI ID；评分层仍会复核名称和城市后再取分，不会仅凭 ID 盲目继承，也不会把母景区与子景点混为一条。

高德开放平台当前的 POI 2.0 接口可返回景点类 POI 的 `business.rating`，但需要申请 Web 服务 Key。在项目根目录 `.env` 写入 `AMAP_WEB_SERVICE_KEY=你的Web服务Key` 后，总控优先使用已绑定 POI ID 的详情查询，否则执行城市限定文本检索；名称、城市和唯一性仍需全部通过。实时接口失败或未配置 Key 时自动退回本地历史快照，不阻断其他资料补全。
- 不把模板卡片、占位文字或“补全中”称为完成。
- 不在点点登录失效或回答不完整时写库。
- 不自动提交或推送 GitHub；最终上线由用户确认。
