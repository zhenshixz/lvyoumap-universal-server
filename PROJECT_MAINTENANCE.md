# 中国旅游地图项目与数据维护手册

最后更新：2026-08-10

这份文件是项目的长期上下文和操作入口。以后修改景点、图片、攻略或部署流程前，先读本文件；不要只凭聊天记录恢复规则。

## 1. 当前产品和仓库边界

- `D:\github\lvyoumap-universal-serverbeta`：本地试验版，所有数据迭代先在这里完成和验收。
- `D:\github\lvyoumap-universal-server`：正式 Git 仓库。只有 beta 验收通过后，才做窄范围同步并由用户决定提交、推送。
- `D:\github\lvyoumap`、`D:\github\lvyoumap-edgeone`：历史参考，只读，不再作为新数据的直接维护源。
- 正式站点为 `https://xzmap.xzbest.site`，通用服务器版不绑定阿里云、腾讯云、宝塔或特定 CDN。

禁止把整个 beta 文件夹覆盖到正式仓库。正确顺序是：beta 修改 → 本地验证 → 查看差异 → 仅同步确认过的文件 → 用户用 GitHub Desktop 提交和推送。

## 2. 运行架构

- 页面、地图、景点 JSON 和本地图片由 `dist` 静态提供。
- Node.js 只承载 `/api/health` 和 `/api/weather`，因此实时天气仍然存在；它不是纯静态网站。
- `scripts/build.js` 在部署时生成 `dist`，平时不需要手工压缩上传。
- 云服务器轮询 GitHub 时只在提交发生变化后拉取、构建、健康检查和原子切换；未更新时只是一次轻量提交号检查。
- `content/db.json` 是历史全国基础库，不进入公开产物；不要为了补一个景点直接批量重写它。

## 3. 数据分层与优先级

构建时按以下顺序合并，后层覆盖前层：

1. `content/db.json`：历史全国基础数据。
2. `content/manual-attractions*.json`：经过人工核验的新景点；贵州核心补充在 `manual-attractions.guizhou-core.json`。
3. `content/attraction-overrides.json`：给已有景点补字段，不复制整条记录。目前用于遵义会议会址。
4. `content/lazy-guide-overrides.json`：小红书点点采集结果，只允许覆盖 `lazy_ai_text` 和 `lazy_ai_source`。
5. `content/image-overrides.json`：已核验的图片替换。

生成文件是 `data/provinces-index.json`、`data/provinces/*.json`、`data/search-index.json`。这些文件只能由生成脚本更新，不手改。

## 4. 一个核心景点的完成标准

新增景点必须一次完成三块，任何一块缺失都不能算完成：

- 基本信息：名称、城市、介绍、等级、地址、开放说明、票务说明、提示、至少两个来源、图片来源和许可证。
- 旅行指南：四季穿衣、外部到达、景区内部交通、住宿区域、至少三项当地食物、老人和儿童建议。
- 懒人攻略：前端优先展示完整的 `lazy_ai_text` 文章；同时保留至少两条结构化 `lazy_routes` 作为来源明确的兜底。

文章式懒人攻略至少应包含：明确游览顺序、老人或儿童适用性、省力交通或少走路办法、时长或折返点、风险与当日公告提醒。只有“路线一/路线二”两张模板卡但没有文章，不算完成。

票价、开放时间、接驳和演出属于易变信息。不得写成永久事实，必须标注“以官方当日公告为准”，并更新核验日期。

## 5. 小红书点点攻略机制

历史机制已在通用服务器 beta 中恢复，日常入口统一为 `数据维护总控.bat`。总控现为全国长期维护版，支持扫码登录、全国增量、指定省份增量、后台运行、一次/持续查看进度、安全停止、全国统计以及生成校验。以后正常维护不需要再通过对话临时运行脚本。

推荐操作顺序：

1. 首次使用或提示登录失效时，选择“扫码登录”。
2. 日常按省治理时选择“指定省份增量更新”；已建立好全国治理清单后也可选择“全国增量更新”。
3. 采集在后台运行，总控可以关闭；再次打开仍能查看进度或安全停止。
4. 成功结果逐条保存，任务结束后自动重新生成静态数据；失败和未完成项不写入，下次运行自动续补。

也可以使用以下底层命令：

```powershell
npm run xhs:login
npm run xhs:list:guizhou
npm run xhs:guizhou
npm run xhs:stats
npm run xhs:global
```

固定提示词沿用旧版：

```text
景点名 长辈小孩省力路线，不要无关内容（如餐饮推荐）
```

采集规则：

- 使用 `.runtime/xhs-profile` 保存本机登录状态，该目录不提交 Git。
- 默认只处理没有点点来源或标记“待点点复核”的景点；已有正常点点文章全部跳过。
- 回答长度、路线信号、老幼适用性、安全提醒和文章结构都通过后才写入。
- 登录失效、回答截断或质量不足时不写入；进度和失败样本在 `.runtime` 中保留。
- 每条成功结果立即原子写入 `content/lazy-guide-overrides.json`，首次写入前创建本地备份。
- 采集器绝不修改 `content/db.json`，也不覆盖景点的基本信息、图片或旅行指南。
- 不绕过二维码、验证码、平台风控或账号验证。
- 总控不提供强制覆盖入口，避免误刷新已经合格的攻略；开发者底层的 `--force` 只用于明确审计后的人工修复。
- `.runtime/xhs-lazy-progress.json` 保存跨窗口进度，`.runtime/xhs-lazy-process.log` 保存后台日志，`.runtime/xhs-lazy-stop.flag` 用于在当前景点完成后安全停止。

`lazy_ai_source.source` 的含义：

- `xiaohongshu-dian-dian-ai-chat`：已由点点实际生成并通过机器完整性检查。
- `multi-source-editorial-pending-xhs-verification`：根据官方和公开旅游资料整理的可用稿，仍需点点复核替换；不能冒充小红书结果。

## 6. 贵州试点状态

贵州核心清单维护在 `content/core-attractions.guizhou.json`，当前共 15 个：黄果树、梵净山、荔波小七孔、西江千户苗寨、织金洞、中国天眼、青岩古镇、万峰林、肇兴侗寨、遵义会议会址、赤水丹霞、百里杜鹃、龙宫、镇远古城、马岭河峡谷。

2026-08-10 已完成：

- 核心覆盖由 10/15 提升到 15/15。
- 西江千户苗寨补齐基本信息、旅行指南、结构化路线和文章式懒人攻略。
- 新增中国天眼、万峰林、肇兴侗寨、镇远古城、马岭河峡谷完整记录和可授权图片。
- 遵义会议会址通过覆盖层补齐基本信息与旅行指南。
- 新增严格质量报告，核心景点必须同时通过基本信息、旅行指南、懒人攻略和来源检查。

贵州全库里仍可能存在车站、普通广场等低价值 POI。它们不应为了追求“所有记录100%”而自动生成攻略；核心清单和真实游客价值优先。

## 7. 验证和回撤

每次数据改动后执行：

```powershell
npm run verify
```

验收包括 JavaScript 语法、静态数据生成、部署产物检查和贵州核心严格质量报告。报告写入 `reports/core-attractions-guizhou.json`。

上线前还要人工抽查：

1. 搜索新增景点能显示正确省份和城市。
2. 基本信息、旅行指南、懒人攻略三个标签都有真实内容。
3. 图片主体与景点一致，点击大图后清晰。
4. 手机、平板、PC 的列表与详情不溢出。
5. 实时天气接口仍可访问。

回撤优先删除或恢复小型覆盖层文件，不回滚整个基础库：

- 攻略问题：恢复 `content/lazy-guide-overrides.json`，本地备份在 `.runtime/backups`。
- 已有景点字段问题：恢复 `content/attraction-overrides.json`。
- 新增景点问题：只撤回对应 `manual-attractions.*.json` 记录和图片。
- 图片问题：只撤回对应图片覆盖项和图片文件。

## 8. 后续推广到全国的顺序

先验收贵州，再逐省推进：

1. 建省级核心清单，官方文旅或A级目录确认身份，OTA和大众点评只辅助判断游客认知与口碑。
2. 生成缺失报告，不以总记录数代替核心覆盖率。
3. 逐个补齐完整三块数据和图片授权。
4. 仅对新增或待复核景点运行点点采集，不刷新已有正常文章。
5. 运行严格报告和跨省回归，再进入正式仓库。

“全国增量”是长期自动补缺能力，不代表应首次就无人值守地改全国 5000 多条记录。建立核心清单和数据质量标准仍应按省推进；总控会跳过已有合格结果，并且所有写入只进入可回撤覆盖层。

## 9. 常用命令

```powershell
npm install
npm run check
npm run report:core
npm run quality:guizhou
npm run verify
npm start
```

Windows 用户也可以双击：

- `start_dev.bat`：用户已有的本地开发入口，保持不变。
- `数据维护总控.bat`：全国中文总控，可做点点登录、全国/指定省份后台增量采集、实时进度、停止、统计和完整质量检查。

## 10. 维护红线

- 不直接改正式服务器或正式仓库来试错。
- 不整库覆盖，不把生成文件当内容源。
- 不用未经授权或来源不明的图片替换现有素材。
- 不把 OTA 评分、点评量、票价视为永久事实。
- 不把模板卡片、占位文字或“补全中”称为完成。
- 不在点点登录失效或回答不完整时写库。
- 不自动提交或推送 GitHub；最终上线由用户确认。
