# CODEX MARK - 项目关键变更记录

## 2026-09-15｜阻止已删除景点重新进入图库清单

- 发现 `amap_B0FFF43GC4` 删除后仍出现在新清单：原因是图库待补池 `.runtime/attraction-gallery-batch/state.json` 仍保留旧条目，生成逻辑直接读取该池，不依赖 `content/db.json`。
- 已从待补池、未运行清单、当前清单及发现历史中清理该 ID；新增 `content/attraction-exclusions.json` 作为 Beta 删除墓碑，图库清单生成会永久跳过其中的 ID，避免旧状态恢复后再次出现。
- 保留图片缓存和历史回滚备份；当前工作台接口返回清单 199 项、目标 ID 命中 0，待补池不再返回该景点。正式仓未动。

## 2026-09-15｜修正 Beta 到正式仓同步白名单

- 检查 `sync_to_formal_git.bat` 的实际预览：图库更新集中在 `content/attraction-gallery-overrides.json` 与生成后的 `data`，会按文件哈希校验并复制；本地 `assets` 二进制图片与正式仓当前无差异。
- 修正 `scripts/promote_to_formal_git.ps1` 根目录白名单，补入当前使用的图库、后台、进度、停止、诊断和内容维护 BAT，并移除已删除的旧入口；同时将 `.backup` 命名纳入备份排除规则。
- 预览模式通过：当前待同步 61 个文件、约 93.39 MiB；正式仓无未提交改动。同步脚本仍只复制/更新白名单文件，不会自动删除正式仓残留旧文件；正式仓未修改。

## 2026-09-15｜清理 9 月前旧备份

- 用户确认清理 2026-09-01 之前的旧备份。已删除 `.runtime/backups` 下 5 个 8 月内容回滚目录、15 个 8 月懒人攻略备份，以及 `.runtime/promotion-backups/20260825-162508`，共 59 个文件、约 56.39 MiB。
- 保留 9 月 1 日及之后的内容回滚包、9 月 8–14 日正式同步回滚包、9 月 10 日命名的代码备份、图库当前批次备份和 9 月 11 日整理出的 `old-backups.zip` 压缩归档；未删除线上图片、正式数据或当前任务状态。
- 清理前已检查当前 `.runtime` 状态与脚本引用，未发现对已删除备份的运行时依赖。仅修改 Beta，正式仓未动。

## 2026-09-15｜清理冗余根目录 BAT

- 用户确认删除三个已被当前流程替代的入口：`run_gallery_batch.bat`（旧两阶段图库采集）、`refresh_gallery_delivery.bat`（旧周期性交付预览）、`start_universal_server.bat`（与 `start_dev.bat` 重复的旧服务启动器）。
- 当前入口保留 `gallery_batch.bat`、`run_gallery_background.bat`、`view_gallery_progress.bat`、`stop_gallery_background.bat`、`preview_gallery_batch.bat` 及数据维护、同步和诊断 BAT；`.runtime/gallery-link-batches/legacy-launchers` 归档文件未动。
- README 的当前 Windows 启动说明已改为 `start_dev.bat`；历史记录中对旧入口的引用保留为变更档案。仅修改 Beta，正式仓未动。

## 2026-09-15｜删除异常景点歪脖老母

- 用户确认删除异常记录 `amap_B0FFF43GC4`。该记录原始名称已损坏，且人工覆盖资料错配到其他景区，直接保留或只修名称均不可靠。
- 已从 Beta 主内容库、基础信息覆盖、展示标签、辽宁静态数据、搜索索引、当前图库清单及后台调度状态中删除；对应图片为远程高德链接，没有删除本地图片文件。
- 新增 `scripts/remove_attraction_by_id.js`，以后可按明确 POI ID 同步清理 Beta 的权威内容层、当前图库清单和后台状态，避免残留再次生成。
- 静态数据与 Beta 前端已重新构建，目标 ID 在所有当前运行数据中均为 0；图库工作台服务 PID 28048 正常，后台无 worker。历史恢复备份保留，正式仓未动。

## 2026-09-15｜移除新清单100条上限

- 用户要求清单条数不设人为上限。前端移除 `max=100`，服务端生成和保存校验改为任意大于0的整数；输入超过当前剩余量时生成全部剩余项。
- 保存请求体上限从256KB调整为8MB，支持当前千级待补清单保存；仍保留当前剩余池作为自然上限，当前为1668条。
- 200项清单校验、工作台/worker回归、Vue模板及服务健康检查通过。未代用户生成新清单，Beta服务已重启，正式仓未动。

## 2026-09-15｜补充Trip名称后缀别名搜索

- 用户发现“贵阳百花湖风景名胜区”用完整名称无结果，但“贵阳百花湖”可定位详情页。自动发现现在在原名无匹配时，按顺序尝试去掉通用后缀的变体：风景名胜区、国家级自然保护区、自然保护区、风景区、景区、旧址、遗址；变体还会在必要时去掉城市前缀。
- 名称匹配仍要求搜索结果地域与景点城市一致；不改变清单名称，不猜测具体别名。实际查询词、查询变体、候选及 `first_same_region_alias` 规则会写入 discovery 记录，便于追溯。
- 发现策略版本升至5。旧版本“无匹配”自动发现项会在后续自动续跑/生成时重新评估；同一已成功访问的详情URL仍受历史保护，图片已全部不合格不会重复浪费请求。
- 回归验证覆盖“贵阳百花湖风景名胜区”→“百花湖风景区”同城匹配、策略/图库/写入测试及Vue模板；Beta服务已重启，正式仓未动。

## 2026-09-15｜修复写入成功后顶部短暂连接报错

- 截图中的“已写入 Beta”是真实成功回执：批次 `20260914-160643-44ab79` 已写入83个景点、224张图。顶部 `Cannot read properties of null (reading 'attractions')` 是写入构建期间页面轮询读到正在替换中的省份 JSON 空值，不代表写入失败，也不需要回滚。
- `gallery_existing_images.existingMap` 现在对构建替换窗口中的空 JSON 安全处理；页面刷新成功后会清除已经恢复的连接错误，避免成功状态旁长期残留红色提示。
- Beta 服务已重启，接口返回批次 `apply.status=applied`；空值模拟、工作台写入回归及服务健康检查通过，正式仓未动。

## 2026-09-15｜HTTP 432 改为整批暂停，等待用户切换 IP

- 用户指出连续请求下一个景点会大概率继续触发432，冷却反而扩大风控窗口。现改为 Trip 详情页或 SHOP 图库接口首次HTTP 432即暂停整批，不再继续请求后续景点，也不在同一IP上自动重试。
- 进度页显示“Trip 返回 HTTP 432，批次已经暂停”，浏览器标题加警告并弹出一次提醒；新增“我已切换 IP，继续运行”按钮。该按钮从断点恢复，成功项、已有候选和当前清单均保留，不重复下载。
- 普通“自动搜索/续跑”按钮不能绕过 `ip_blocked` 状态，必须完成换IP后走专用按钮；已写入Beta的批次不允许继续采集。
- 当前批次 `20260914-160643-44ab79` 已停止，断点59/100，触发项为旗山万佛寺，后台无worker；Beta服务已恢复并健康检查通过。未自动续跑，正式仓未动。
- Node语法、重试策略和Vue模板检查通过。
- 已验证普通续跑接口在 `ip_blocked` 状态下直接拒绝，不会绕过暂停；只有专用换IP断点接口可恢复。Beta服务已重启，正式仓未动。

## 2026-09-14｜HTTP 432 再缩短为5秒一次重试

- 用户要求进一步提高吞吐：Trip详情页及SHOP接口第一次返回HTTP 432后只等待5秒，再请求1次；第二次仍为432即记录并跳过当前景点，批次继续。
- 正在运行的批次 `20260914-160643-44ab79` 已在保留成功候选和断点后重启worker，新PID 20624已显示“Trip风控冷却5秒”。续跑会重新打开此前网络失败项，因此完成计数短暂回退；成功项及候选不重复下载、不丢失。
- 重试常量、策略测试与Node语法检查通过；只改Beta，正式仓未动。

## 2026-09-14｜清单页轻量显示剩余待补数量

- 在“新清单条数”输入框旁增加“尚有 N 条待补可生成”，复用现有队列过滤结果，不扫描或加载图片。
- 数字排除已入库、已处理、待验收、正在运行及被未运行清单占用的景点；生成、保存或恢复清单后随接口结果立即更新。
- 当前剩余量为1768条，当前清单16项另行保留。Node语法、Vue模板和服务健康检查通过；只改Beta，正式仓未动。

## 2026-09-14｜统一已填链接与已自动选链接的续跑语义

- 用户在16项清单点击“仅按手填链接采集”后，最后一个自动选择项“集安市大吉他主题广场”没有产生候选。核查发现两个原因：手填模式旧实现不采用空输入框下的自动候选；该景点实际已在批次 `20260914-150812-436b18` 采集同一Trip详情页，来源仅提供1张2400×1600图片，因“画面过暗”未纳入，同链接历史保护随后阻止重复下载。
- worker现会在历史判断前采用清单中已确定的自动候选。因此按钮更名为“采集已填及已自动选链接 / 续跑”，尚未跑过的已自动选项目无需启动完整搜索也能执行。
- 清单展示会识别自动候选是否已经成功访问过：已耗尽的同一URL不再显示“可自动采集”，而明确显示来源数量、拒绝原因及“重复运行无效，请更换链接”，防止假启动和无效请求。
- 当前景点显示为“已采集（来源提供1张），未纳入原因：画面过暗”，并未再次浪费请求。语法、Vue模板、重试及worker回归通过；Beta服务已加载，正式仓未动。

## 2026-09-14｜HTTP 432 改为快速失败，并提供换 IP 后定向重跑

- 用户确认不再采用较长自适应冷却。Trip 详情页及 SHOP 图库接口遇到 HTTP 432 后固定等待 10 秒，重试 2 次；第三次仍失败即记录并跳过当前景点，批次继续处理后项。
- 已完成且尚未写入 Beta 的批次，在进度页顶部显示“切换 IP 后快速重跑 N 个失败项”。按钮只恢复 HTTP 432、连接超时等临时失败项，直接复用该批 input 和已发现 URL；成功项、明确无图库项和人工待补项均不重复处理，也不依赖当前清单是否已更换。
- 最新批次 `20260914-150812-436b18` 已自然完成，共 100 项、221 张候选、74 个景点有候选；当前按钮识别 3 个 HTTP 432 项。未自动点击重跑，等待用户切换 IP 后操作；批次尚未写入 Beta。
- Node 语法、重试策略、图库 worker、写入保护、Vue 模板及服务端 API 回归通过；Beta 服务已重启并返回 `retryableCount=3`，正式仓未动。
- 用户切换 IP 后已点击快速重跑，3 项全部成功：格凸河风景区 5 张、青龙桥 2 张、黄岗梁森林旅游区 5 张；本批候选增至 233 张、77 个景点有候选，待人工预览，尚未写入 Beta。
- 修复重跑名单不可见：后续启动时持久化本次重跑 ID、名称和原失败原因；进度表顶部列出本次名单，将对应行置顶并以黄色“本次重跑”标记。当前批次的3项追溯记录已补齐，刷新进度页即可看到。

## 2026-09-14｜HTTP 432 实测与自适应冷却

- 100 项批次 `20260914-135637-beff32` 用时约 10 分 29 秒；首次 432 出现在第 85 项。此前完成 84 项、约 99 次搜索导航、62 个详情页成功采集。最终 5 个详情页遇到 432，但错误之间仍有其他详情页成功，不能解释为达到固定次数后整个 IP 永久封禁。
- 14:15 程序端复测上海滨江森林公园二期仍返回 432/17 字节 `whaleguard block`；约一分钟内同 URL 再次请求恢复 200，而另一详情页同时可能返回 432。普通浏览器会话与 Node/无头浏览器表现也不同。结论：WhaleGuard 是请求/会话级短时风控，暂无证据支持固定触发时长或永久封禁。
- worker 将 Trip 详情页 HTTP 432 纳入可重试错误：首次等待 15 秒，连续触发等待 45 秒，每页最多 3 次；状态页显示冷却阶段，并记录累计次数、最近触发与恢复时间。最终仍失败则保留为网络风控可重试项，不判无图、不要求人工找新链接。
- 重试策略同步把 432 归入 transient，后续待补队列可自动重试。语法、worker 离线回归与重试策略回归通过；未重跑当前批次、未写入内容、正式仓未动。

## 2026-09-14｜增加名称别名搜索

- 对名称含中点/括号的景点，整句无候选时自动拆分别名依次搜索，再用地域归一和名称包含规则选第一项；覆盖“呼伦贝尔大草原·莫尔格勒河景区→莫尔格勒河景区”“油麻地警署(旧址)→油麻地警署”。点击自动搜索时执行并记录查询、候选和匹配规则，无需手填。
- 回归通过，Beta 服务已重启，未启动批量任务、正式仓未动。

## 2026-09-14｜Trip默认占位图判无图，退出补录流程

- 用户确认圆形图标遮挡的默认占位图无需人工整理。实读六厂广场Trip 145656619：poiImageCount=0、overview.imageInfo.imageCount=0、showGallery=false，同时poiImage仍有defaultUrl（0102u1200086k28vc0A5C.jpg，500×500）。因此不能仅凭poiImage非空当成照片，也不按图片里是否有圆形误杀正常照片。
- parser用上述三个结构化条件明确标记noImageConfirmed，返回空图库和证据；worker记录no_image_available/closed，不下载默认图、不进入自动待补或手填清单。当前图源无图不等于断言所有网站永远无图；不同的新详情URL仍可再次尝试。
- 已用真实页面证据关闭历史六厂广场记录，保留原state备份于.runtime/gallery-link-batches/placeholder-probe；当前23项清单保留原ID和数据，只从填写展示中隐藏已关闭项。原有Beta图片、已验收内容、正式仓未改。
- 同步修复此前显示排序后按数组下标校验保存的隐患：保存按唯一ID对应，仍拒绝缺ID/重复ID，避免排序导致保存错误或链接错配。
- 验证真实占位页面及模拟正常/字段缺失图库、关闭项排除、新URL可重试、排序后保存、Vue模板及两套图库回归通过。没有启动批量采集。

## 2026-09-14｜移除图片尺寸限制

- 按用户确认，固定图源图片不再因宽度或高度被筛掉；quality脚本取消尺寸门槛。仍保留图片协议/来源白名单、响应大小、解码、过暗、近似重复检查。
- 旧的“尺寸不足”记录在下一次重跑时会重新下载/评估；当前已完成批次不自动改写，避免无授权改变验收结果。三套回归及Python语法检查通过，仅改Beta。

## 2026-09-14｜修复短景点名后缀匹配

- “红门”对应“泰山风景名胜区-红门”/“红门宫”时，旧逻辑因短名称限制仍要求手选。已允许至少2字符的连续名称片段作为保守后缀匹配，地域仍必须归一后一致；按结果顺序自动选第一项并记录模糊规则。
- 当前接口已验证红门自动标记 `first_same_region_fuzzy`，服务已重启。未启动任务、未改正式仓。

## 2026-09-14｜地域别名归一，自动项不再要求手填

- 修复“阿勒泰 vs 阿勒泰地区”“黔南 vs 黔南布依族苗族自治州”等地域字段差异：去除地区、市、自治州及自治区后缀，并允许地域键相互包含；仍要求名称有合理重合。
- 当前 draft 中“阿勒泰地区布尔津五彩滩”和“小七孔景区”已自动标记 `first_same_region_fuzzy`，会直接采集；页面只保留搜索记录，不再显示填入按钮。
- policyVersion 升至4，旧手工记录自动重新评估；三套回归通过，服务已重启，未启动任务、正式仓未动。

## 2026-09-14｜补充保守模糊名称匹配

- “阿勒泰地区布尔津五彩滩”可对应同城搜索结果“五彩滩”，已升级 Trip 选择为 policyVersion=3：地域必须一致，名称支持完整名相互包含且长度至少3字符；按搜索顺序选第一项并记录规则、排名和候选。
- 旧 policyVersion 2 的手工项会重新评估；无可解释名称重合仍留人工。回归通过，服务已重启，未启动新批次、未改正式仓。

## 2026-09-14｜修正清单排序未实际生效

- 用户发现人工项与自动项仍交错。复核确认前次排序说明已写入但 `draftView` 实际仍是旧实现，属于代码替换遗漏。
- 已真正加入排序：API 返回清单时人工/手填项在前，autoSelection 项在后；只影响展示数组，未改变 draft 文件、ID 和 worker 顺序。当前 23 项接口验证首个自动项索引 12，之前 0–11 均为人工项。
- 已重启 Beta 服务并健康检查通过，未启动任务、未修改正式仓。

> 本文档是 Codex 在本项目中的唯一长期记录。按日期降序维护，只记录关键结论、改动文件、验证结果和必须继承的规则。
> `docs/GEMINI_MARK.md` 由 Gemini 独立维护，Codex 不修改。

---

## 2026-09-14｜修复旧手填清单仍显示“需确认”的遗漏

- 用户截图双廊古镇、南天湖、三亚·中交凤凰岛仍要求手选。原因：23项旧draft保存了旧discoveryReason/candidates，之前只更新匹配与重试策略，没有更新清单展示。
- gallery_trip_discovery.draftView在读取/保存/生成/恢复的API响应中按当前规则计算autoSelection，不改用户url或savedAt。页面明确显示同城市首项名称、地区、排名与“无需手填”，搜索记录折叠为回溯用途，自动项隐藏逐项填入按钮。
- worker使用同一select函数直接采用draft已有的同城候选，记录reusedDraftCandidates、selectedAt与选择规则；保留手填链接优先与历史成功来源不重采保护。不必为了旧候选重新搜索。
- 验证：当前23项中11项可自动选取，截图3项均命中各自第1项，draft未被修改；图库与重试回归通过、Vue模板编译通过，服务健康检查正常。没有启动采集或写入内容，正式仓未动。

## 2026-09-14｜放宽图片宽度门槛与同城首项自动选择

- 图片质量门槛调整：Trip/高德均按解码后的原始宽度 `>540px` 放行，不再要求1000/1280宽及最低高度；过暗、近似重复、无法解码仍拒绝。记录qualityPolicyVersion=2，旧的“尺寸不足”记录在生成待补队列时可重新评估。
- Trip搜索调整：只要搜索结果的城市字段与景点城市一致，就按结果顺序自动选择第一项，即使名称有前缀/后缀也继续采集；记录候选列表、选择排名、名称是否一致和selectionRule，便于回溯。没有同城市结果才留手填。
- 旧的同城歧义项不再永久卡住：policyVersion<2的manual记录会按新规则重新评估；同一Trip链接已成功尝试且图片质量确实不合格仍不重复下载。
- 回归通过：三套图库/工作台/重试策略测试，Vue模板及Python语法检查通过。只改Beta，未启动新批次，正式仓未动。

## 2026-09-11｜统一待补队列，按失败原因分流，已耗尽Trip链接不重跑

- 用户明确：只需“待补景点”入口，成功访问Trip但图片不合格的同链接无重复采集价值。已移除“尚未处理／之前无图失败”的手动分类，生成待补清单自动优先新景点，再纳入网络异常项目，已入库/待验收/正在处理/预留清单继续排除。
- 新增gallery_retry_policy.js：无图、已下载但全部质量不合格、无名称地域匹配留人工；网络超时、连接错误、429/5xx及图片下载网络失败可重试。手填模式留空但尚未自动搜索的项目仍允许首次搜索。不能把所有collected都当成耗尽，需区分图片网络下载失败与实际质量拒绝。
- 双层保护：队列生成过滤+worker启动前跨批次检查同景点同规范化Trip URL。成功尝试的同链接即使重新保存旧清单也不再采集；更换不同详情链接可采集。自动搜索未匹配或同来源已耗尽，不反复搜原名称。此前完成但网络异常的批次可点启动续跑，只重试异常项目。
- 自动重试生成清单保留原已确认/已匹配URL，不丢链接重新搜索。没有启动新批次，没有改当前draft/用户输入、内容和正式仓。当前服务已加载修复，刷新工作台生效。
- 验证：策略测试覆盖质量耗尽、URL查询参数归一、换链接、连接失败、503、空图库、未匹配；worker回归覆盖跨批次成功URL不重采、续跑及零高德请求；写入旧图保护回归及Vue模板编译通过。主要文件：gallery_retry_policy.js、gallery_link_batch_actions.js、gallery_link_batch_worker.js、gallery_link_batch_ui.js、gallery-link-batch.html。

## 2026-09-11｜恢复今天误覆盖的旧图；补图只能追加，保护已有验收结果

- 教训／长期强制规则：补图不等于替换图库。“写入Beta”只授权加入本次勾选新图，不授权移除已有图。旧图与新图必须分区标注；已有图强制保留，默认保留当前封面，只有明确选择其他封面才切换。删除已验收图片须另行明确授权，不能通过后续批次少选图片或空列表间接删除。数量上限只作用于新增图，不得截断已有图库。
- 全日追溯：上午全国图库applied回执84项，以及工作台4批9+4+6+8项，共111个景点；不是把历史312项误当作今天全部新增。上午依据写入前overrides备份，缺少图库项时结合9月10日保留的构建data；工作台依据各批apply-backup/data与overrides精确还原写入前状态。
- 找到44个景点各少1张旧图：上午17项、工作台27项。旧图均未命中拒绝清单，无本地图片缺失。已在Beta恢复44张，合并保留当前已验收的新图与封面；上海滨江森林公园现为旧高德+新Trip共2张。没有删除或重新下载原图、没有操作正式仓。
- 恢复通过gallery_link_batch_apply的备份/构建/核对/切换事务完成，44项最终共198张。复核111项待恢复为0；工作台今日验收86张新图全部仍在。恢复证据与回执：.runtime/gallery-link-batches/recovery-20260911/{audit.json,apply.json,apply-backup,build.log}。一次性审计/恢复脚本：scripts/restore_gallery_images_20260911.js，默认只审计，--apply才执行；再次执行不重复加图。
- 核心修复：gallery_existing_images.js统一提取旧封面/图库、追加与去重；gallery_link_batch_apply.js写入时重新读取当前旧图防止旧审批覆盖新验收结果，同时核验构建最终并集。回执分开记录本批选择与最终图库。旧apply_attraction_gallery_batch.js、apply_attraction_gallery_pilot.js一并改为保留旧图，避免旧入口重演。
- 验收页与API新增existingImages/existingCover，分区展示“已有图片·自动保留”和“本批新图”，已有图不能在补图流程中取消；两区可明确选择封面。只勾选新图也不会移除旧图。服务已加载修复。
- 验证：离线回归通过，覆盖仅有旧封面、旧图库保留、去重、显式换封面、非法封面拒绝、审批后有新入库图仍保留、幂等及失败恢复；实际构建和逐景点图序核验通过；Vue模板编译通过。

## 2026-09-11｜核对上海滨江森林公园旧图被新批次替换

- 用户反馈写入后只剩新图。核实批次20260911-170147-f322fe于17:03:31写入该景点1张Trip图；写入前apply-backup/data/provinces/shanghai.json中该景点只有1张高德image封面，无images数组、无既有图库override。早先beforeSelected=2是历史隔离候选数量，不是当时前台已发布图片数。
- 原因：gallery_link_batch_apply.js execute将选中图直接赋给image及images，并校验构建结果严格等于所选列表，实际语义为整组替换，不是追加补图。旧高德封面URL仍保留在本批apply-backup数据，可恢复；未请求图源、未删除图片文件。
- 本轮仅诊断，不擅自改封面或恢复旧图。建议后续验收同时呈现旧图与新图，让用户选择最终保留集合，避免“写入”含义不清导致旧图无声消失。

## 2026-09-11｜自动搜索首批已由用户写入，保留后续补录入口

- 确认批次20260911-164504-f1d45b于16:52:42成功写入Beta，6个景点17张图，apply回执为applied；当前无采集worker运行。
- 修复写入后“将本批无图项转为手填清单”按钮被未写入条件隐藏的问题；按钮在已完成批次始终按无图项显示，剩余14项可继续手填。仅调整工作台HTML，未代为生成或启动下一批，正式仓未动。

## 2026-09-11｜Trip站内自动搜索接入工作台，当前20项实跑完成

- 用户授权实现并用当前清单试跑。新增 gallery_trip_discovery.js：本地 puppeteer-core/Chrome 无头浏览器独立配置、串行真实渲染搜索，提取 attraction/shops 卡片名称、城市及真实详情URL；不依赖对话模型逐项找链接，也不复用小红书会话。
- 自动搜索与仅手填模式分开；已填链接优先，空地址自动搜索须唯一的完整名称及地域匹配。只做简繁/标点归一；同名异地加一次城市+原名查询，不自动删后缀或猜别名。成功映射缓存，详情图沿用既有下载/质量/隔离预览流程。
- 有限超时和两次尝试，连续两项搜索服务失败暂停保留断点；图片/来源/项目检查点和互斥锁复用。自动发现URL与原始input快照分开比较，避免点击保存未改内容后重复采集。电脑重启或验证码不保证自动恢复，需用户重开BAT续跑。
- 工作台增加自动搜索按钮、搜索原因显示、无图项转手填清单、少量备选页面“填入”按钮；保存仍代表人工确认，写入仍必须用户点击。新依赖检查加入puppeteer-core。说明见 docs/GALLERY_LINK_BATCH.md。
- 实跑批次 20260911-164504-f1d45b：当前revision5的20个空地址景点，首轮48.86秒完成，6项匹配/13张；再只补测3个重名项的城市查询，累计8项匹配详情、6景点17张合格图，2项仅有尺寸不足图、12项未匹配，共14项待人工。全程无服务错误、高德零请求、未写入content或正式仓。实测未达到80%自动覆盖，当前主要限制为别名与严格实体匹配。
- 有图：长春水文化生态园2、可可西里4、前滩休闲公园1、伟大转折剧场5、乌兰木伦湖1、十堰人民广场4。开封世纪广场及钟山市民广场有详情但图片尺寸不合格。沙面岛/沙面、阳明山公园/阳明山国家公园等仍待人工确认，不降低质量。
- 两套离线回归通过，新增同名异地/子景点/歧义拒绝、手填优先、自动发现到采集、续跑不重下载、连续断线暂停及释放锁验证。实际网页进度显示正常；全国旧任务继续暂停。运行证据：.runtime/gallery-link-batches/runs/20260911-164504-f1d45b/{state.json,input.json,manual-pending.json}。

## 2026-09-11｜接通Trip SHOP全部相片批采

- 从用户士林夜市示例页的实际脚本定位全部相片接口/restapi/soa2/27316/SearchPoiImageInfo，POST poiId、videoId=null、locale=zh-HK、pageIndex=1、pageSize=20；无浏览器登录或cookie即可HTTP200返回图库。
- 新增parseTripShopGallery：核对详情页结构化poiId、接口resultCode，提取imageInfo原始Trip CDN地址并过滤去重。worker对shops采用此接口，最多下载前5张，沿用既有尺寸/过暗/去重筛选与隔离预览，不翻页扫全库、不混入推荐地点。
- 实测士林夜市返回64张：探针下载6张（循环末组多取1张，仅隔离测试）；生产worker仍明确截取5张。前5张中4张尺寸合格，第三张684×896未过门槛；澳门渔人码头同接口返回1张。证据保留.runtime/shop-probe/{downloads.json,quality.json}，未写内容或启动批量任务。
- 旧批次unsupported_layout支持再次启动时只重试该项目Trip来源，其他已完成项目和高德不重复执行。离线worker回归含SHOP接口调用、月停用零高德请求，另核对SHOP ID和接口失败拒绝，通过。清单服务已加载新逻辑，正式仓未动。

## 2026-09-11｜保存结果提示放到按钮旁

- 用户反馈保存仅闪一下。核对draft.json确认15:55:27（北京时间）已保存10项清单中的5个链接，revision=4。原成功提示位于下方表格工具栏，不够明显。
- 清单保存按钮旁新增持续可见的已保存链接数量、保存时间和可启动提示；按钮显示已保存，编辑后即时显示未保存状态，保存中另行提示。仅改网页展示，不修改清单或启动任务。

## 2026-09-11｜修复合法Trip购物详情链接被误报HTTPS错误

- 用户截图澳门渔人码头为HTTPS的/travel-guide/shops/macau/macau-fisherman-s-wharf-135951234/；旧校验只接受attraction，且统一错误掩盖实际原因。已允许shops地点详情及合法slug字符，仍限制Trip域名、HTTPS及数字地点ID；错误按协议/域名/详情路径区分，并显示具体景点名称。
- 单次快速验证该shops页HTTP200但没有attraction使用的appData图库。允许保存人工确认链接；采集遇此结构明确记录unsupported_layout并继续其他项目，不冒称已支持购物页取图。未修改用户尚未保存的输入，不刷新其浏览器、不启动采集。仅重启本地清单服务，用户可直接在原页面重新点击保存。
- 定向测试通过：shops、撇号slug、查询参数、域名限制及带景点名错误。正式仓未动。

## 2026-09-11｜Beta冗余与历史备份清理

- 用户授权清理beta冗余文件。扫描活跃脚本/BAT/package引用后，将根目录/content/scripts/docs散落的.bak备份及较早的同步备份压缩归档，保留最近3次同步备份；压缩包逐文件SHA256核对通过后，才删除对应原副本。未删除功能脚本。
- 同时删除已确认业务status=0、infocode=10044的高德失败响应缓存，保留成功POI缓存、冷却状态、任务状态及断点数据。总计清理2544个文件，扣除压缩包后净释放385.5MiB。
- 可恢复归档：.runtime/cleanup/20260911-150358/old-backups.zip；逐文件路径、SHA256和分类见同目录manifest.json。最近3份同步备份保留原目录，当前写入恢复备份未改动。
- 旧预览副本与图库images仍有验收/服务引用，全部保留；assets、content正式数据、图库候选、浏览器xhs-profile会话、.env、node_modules均未清理。之前被审批拒绝的.dist-next保持不动，未尝试绕过。正式仓未修改。
- 清理后4210网页健康检查通过；高德停用策略仍生效，无图库采集worker运行。

## 2026-09-11｜统一图库工作台：可变清单、网页采集、验收写入Beta

- 用户最终规则：保存即确认Trip链接；点击写入Beta即代表所写景点已验收、暂不补图。1–5张均可写入，不沿用自动采集全局3张门槛；0张和未勾选景点保持原图。
- 根目录统一入口gallery_batch.bat（ASCII/CRLF、依赖检查、本机/局域网地址、健康检查）；旧01/02/03 BAT归档至.runtime/gallery-link-batches/legacy-launchers。网页完成生成清单、保存、启动/续跑、进度、选图/封面、写入。局域网只读。
- 清单支持1–100项、默认10；保存历史清单并允许切换。新清单排除已入库、正在处理、待验收及被未运行清单占用的景点。无图/失败通过单独重试条件选取。保存后的同内容已完成清单再次启动直接返回原批次，避免重复采集。
- 新增gallery_link_batch_actions.js和gallery_link_batch_apply.js。写入只采用当前批次勾选的合格图片URL，保留来源，备份图库及data；build.js增加--stage-only，构建并核对封面/图片顺序后切换dist。失败恢复数据，意外中断支持先恢复再重试；approval.json/apply.json按批次保存，成功回执标记暂不补图，重复写入幂等。
- 新增gallery_keep_awake.ps1，采集期间请求Windows保持唤醒，结束释放。高德本月停用策略继续有效，离线测试验证停用时零高德请求。
- 验证：两套离线回归通过（可变条数、历史清单、排除规则、单图写入、越界选择拒绝、幂等、构建失败回滚和中断恢复）；实际stage-only构建成功；浏览器验证生成条数、29→28→29勾选计数及重复启动复用原批次。当前20260911-134827-16c828仍为9个景点29张候选，等待用户点击写入按钮，未代为写入content或启动新采集。正式仓未修改。
- 自动审批拒绝删除本轮构建验证目录D:/github/lvyoumap-universal-serverbeta/.dist-next（仅返回blocked by policy），该目录保留，未继续尝试删除。旧BAT归档成功。使用说明：docs/GALLERY_LINK_BATCH.md。

## 2026-09-11｜本月统一停用高德采集步骤

- 用户确认本月高德基础搜索服务组不可用，要求避免后续批处理继续浪费时间。新增运行时策略`.runtime/gallery-link-batches/source-policy.json`：高德停用至2026-10-01 00:05（北京时间），到期按时间自动恢复，不删除或改写beta中的Key。
- 10项人工链接批处理在停用期不读取高德Key、不发额度探测或POI请求；空Trip地址只运行当时可用的其他来源，可能得到0张。清单页、进度与批次状态会显示高德停用原因，Trip采集不受影响。
- 旧全国图库采集器也读取同一策略；即使误启动，也跳过高德详情、同名POI和子景点高德步骤，包括不复用高德查询缓存。全国任务仍保持暂停，本次没有启动任何采集。
- 语法检查及链接批处理离线回归通过；只修改beta脚本、说明和运行时策略，未写入content，未修改正式仓。

## 长期规则

- 所有开发、修复、采集和验收先在 `lvyoumap-universal-serverbeta` 完成。
- 未经用户明确确认，禁止同步到正式 Git 目录。
- 已完善字段不重跑、不覆盖；修复优先形成全局规则，避免逐条补丁。
- 数据流程允许非关键问题继续运行，失败项断点续跑，只有实体错误、核心字段缺失等关键问题才阻断。
- 对外来源统一展示为：`高德地图 · 景区官方 · 公开百科`；内部真实来源关系保留。
- Codex 后续所有关键变更只追加到本文件，最新日期置顶。

---

## 2026-09-11｜移除已保存Trip链接的名称启发式拦截

- 用户明确：在清单点击保存即代表人工确认链接可用，程序不应再因Trip页面名称前缀、别名或景区范围进行二次阻断。
- worker仍校验链接域名/路径、URL中的POI ID与页面结构化POI一致，并把名称/地域匹配结果作为记录；不再以名称或地域启发式拒绝下载。清单页删除“名称不同”补录区域，明确保存即为人工确认。
- 对旧逻辑已拦截的同批项目提供定向修复：再次运行02时复用原批次，只打开`identity_review`且无候选的项目；已完成景点和已完成来源不重复下载。当前批次仅渔梁坝符合该条件，用户尚未再次启动。
- 离线回归通过：保存链接可跨过名称不一致、定向修复只下载原被拦截的Trip来源、高德不重复请求、批次不新增、raw清理正常。未启动真实采集、未修改content或正式仓。

## 2026-09-11｜免费POI替代源评估：基础数据可替代，免费图片不可等价替代

- 用户反馈新建高德账号受同名实名认证限制，询问类似高德的免费POI。按当前官方接口核对：腾讯位置服务WebService可提供地点搜索/详情的名称、地址、分类、坐标和行政区，适合补POI实体与地域，但标准返回没有景点图片字段；免费额度/QPS以腾讯控制台实际分配为准。
- 百度Place API 3.0支持POI检索，`photos`可返回图片下载链接，但官方明确“获取POI图片”为高级服务，需购买商用授权并联系商务/提交工单开通，不能作为免费高德图片替代。
- Wikimedia Commons/Wikidata可提供授权较清晰的少量景点图片，但小众中国景点覆盖低；Trip精确详情页经本轮验证覆盖与画质更适合作为主图库来源。结论：免费替代组合应为腾讯/现有高德数据做实体校验，Trip做图片，Wikimedia/官网做可选补充；不存在已验证的免费、全国覆盖且直接返回POI图库的高德等价品。
- beta当前没有百度或腾讯Key，也没有对应适配代码。本次仅调研记录，没有申请账号、修改采集流程或正式仓。
- 下月高德5,000次月配额仍够当前约2,027个待补景点各做一次精确ID查询；应保留一景点一请求、命中10044全批停止、缓存永久复用，避免旧全国任务5,460次成功请求和超限后2,110次无效请求重演。

## 2026-09-11｜高德10044最终诊断：基础搜索服务组月配额耗尽

- 用户提供高德控制台截图：基础搜索服务组月调用量5,996、月配额5,000，已使用100%。2025-05-20起高德已取消基础服务原日配额、保留服务组月配额；接口仍返回旧命名的`10044 USER_DAILY_QUERY_OVER_LIMIT`，不能再按旧错误码文字解释为次日恢复。
- 13:58（北京时间）对beta当前配置Key做一次最小实时请求，HTTP 200但业务返回10044；Key字段存在且`AMAP_WEB_SERVICE_KEY(S)`指向同一新Key。基础搜索服务组由关键字、周边、多边形、ID查询和输入提示共享，且API/JS/Android/iOS/微信小程序跨平台共享；创建同账号新Key不会获得新的服务组月配额。
- 11:02新Key曾成功一次、13:48起返回10044，结合控制台5,996/5,000，最合理解释是超限统计/阻断存在同步延迟；不能把一次成功视为新Key获得独立额度。
- 本地`amap-cache`共有5,460个成功POI响应，时间为9月4日至9月9日；其中9月8日15时2,726个、9月9日10时1,879个，已足以解释月配额主体。控制台多出的约536次可能来自其他基础搜索接口/平台，单靠beta缓存无法完全归属。另有2,110条10044失败缓存，是旧批采超限后仍逐项请求造成的无效重试记录，不等于成功计费调用。
- 当前本机无图库采集worker；全国后台仍paused，10项链接批次已完成。历史api-cache有2110条10044，时间跨度9月9日15:16至9月11日09:22（北京时间），说明旧批采此前曾在超限后继续产生大量无效请求；这些旧缓存不代表新Key实际调用2110次。
- 自然恢复时间应为下个计费月，即2026-10-01，而不是9月12日。若要本月立即恢复，按官方现行规则需增加基础搜索流量包/账户可计费余额，或提升开发者认证与配额；具体是否适用以控制台为准。
- 程序当前遇10044后会停止本批后续高德请求，Trip继续，避免再次为每个景点重复打失败请求。本次仅诊断和记录，未改配置、重启批次或修改正式仓。

## 2026-09-11｜首批已完成，随后Trip连接重置

- 用户截图Trip首页ERR_CONNECTION_CLOSED；本机单次Node请求亦ECONNRESET，本地4210健康检查200。证据不足以判定封IP、代理故障或网站故障，不反复探测。
- 用户启动批次20260911-134827-16c828已于13:48:56完成（约29秒），10项处理结束，8项取得24张机器筛选候选；没有因当前Trip断连中断。绶溪未填链接；渔梁坝因解析名“徽州古城渔梁坝和渔梁古镇”与填入别名不同待核对。
- 本批高德首次请求返回10044当日额度不足，其余高德请求按策略跳过；没有取得高德新图。未重启采集、修改清单或发布图片。用户可先查看本地批次预览。

## 2026-09-11｜修复双击BAT的Python定位差异

- 用户双击02时依赖检查失败；当前机器Python312实际已安装Pillow/OpenCV/numpy。原启动器和worker均直接调用PATH中的python，桌面启动环境未必与工具终端一致，旧错误信息没有保留具体原因，不能断言缺包。
- 新增gallery_link_batch_python.js：探测已验证绝对路径、本机Python安装目录及PATH/py候选，实际导入依赖后缓存绝对路径；启动器与worker共用。失败详情写python-check.json。新增run --check-only只检查不采集。
- 验证正常依赖检查通过；移除子进程PATH中的Python路径后仍能定位Python312并导入依赖。未启动采集、未修改用户清单或正式仓。

## 2026-09-11｜交付10项供链批处理入口，由用户手动启动

- 用户明确要求先提供清单网页保存地址，再自己通过BAT启动、查看进度，完成后通过BAT打开按时间区分的隔离预览。本轮未启动真实10项采集，全国后台保持paused。
- 新入口：根目录01_gallery_links.bat、02_gallery_run.bat、03_gallery_preview.bat（ASCII/CRLF、路径依赖检查、错误停留）；服务默认4210起，检查存活后显示本机与局域网地址。Vue日间界面，固定原training 10项，9条已知/用户供链预填，绶溪由用户补填；未改成holdout或擅自扩大范围。
- 脚本gallery_link_batch_common/launcher/server/worker/ui.js、gallery-link-batch.html、gallery_link_batch_quality.py。清单保存在.runtime/gallery-link-batches/draft.json，每批按北京时间生成独立目录和快照；单运行锁、逐源逐图断点、受限超时/重试、失败原因可见。网页保存不会触发采集，已启动快照不受后续编辑影响。
- Trip精确页核对POI、名称/显式别名、前部详情地域后最多5张；高德精确POI只取第一张原图，长边>=1280且短边>=720，Trip沿用1000/560门槛；过暗和跨源感知哈希去重。有几张候选就保留几张，不自动认定3张齐或人工验收完成。别名/大景区范围仍需用户核对。
- beta .env两项AMAP_WEB_SERVICE_KEY(S)已换成用户提供的新Key，未向页面/报告/日志暴露密钥。程序无模型调用、无浏览器搜索或系统定时任务；批处理后台进程关闭窗口仍运行，电脑唤醒由用户保持，重启后需手动续跑。
- 小图预览按批次切换；原始下载在候选及引用写好后仅清理该批raw，不动历史候选或线上图。运行状态、候选、日志均不进入正式仓。
- 验证：JS语法、ASCII/CRLF；离线回归覆盖URL/版本校验、实体拒绝、高德只下载首图、跨源去重、进度恢复不重下载、raw清理；浏览器验证10行清单、保存成功和实际缩略图加载，前端无错误。临时样例及测试缓存已清除，清单恢复为待用户保存确认，真实批次数0。
- 使用说明docs/GALLERY_LINK_BATCH.md；清单服务http://127.0.0.1:4210/#form已启动。未修改content、正式仓或会话数据库。

## 2026-09-11｜新高德Key试查灵湖景区成功

- 用户已删除旧Key；新Key临时试查v5/place/detail成功，精确POI B02400TOA1名称及地域吻合，返回3张原始图片，全部HTTP200并可解码。尺寸和URL保存在.runtime/attraction-gallery-batch/link-pilot/amap-B02400TOA1-live/new-key-report.json；fresh-contact.jpg供人工验收，下载成功不等于质量合格。
- 密钥未写入报告/记录，本次未修改.env（后续运行前须更新旧配置）、未启动后台、未修改图库或正式仓。

## 2026-09-11｜灵湖景区高德查询受额度限制，现有封面可下载

- 精确POI B02400TOA1实时详情仍返回10044 USER_DAILY_QUERY_OVER_LIMIT；此前已确认其他配置Key同日额度耗尽，本次不重复轮询。未找到该POI的amap-cache；state中amapComplete=false、selected为空，不能将其解读为高德无图。
- content/db.json已有一条同POI高德封面URL，实时下载HTTP200，单独生成720px以内缩略图供用户查看。这是现有封面，不是本次详情接口返回图片，当前无法确认高德完整图片数量。
- 产物仅在.runtime/attraction-gallery-batch/link-pilot/amap-B02400TOA1-live/，未修改图库或启动任务。

## 2026-09-11｜后续顺序确认：先完成供链试验，再决定待补数量标准

- 用户决定继续提供此前10项名单中的链接，逐条快速验证能否获取合格图片，沉淀到发现—采集流程；完成机制整理后再试跑，沿用固定试验样本，不跳过验证直接重启全国批采。
- 暂不采用“默认1张即完成”的新规则，暂不调整现有完成判定或据此清空待补队列。每源可贡献少量合格图片、跨源累计，不以单源不足3张判失败。
- 试跑后为最终仍待补景点生成清单网页，展示现有图片数量/小缩略图、已尝试来源、未补成原因，供用户逐项判断是否确有必要补到3张；此时再决定数量例外和后续工作。
- 当前仅记录决策，等待用户继续供链；未启动后台、未修改图库或正式仓。

## 2026-09-11｜绶溪公园高德图片现状核查

- 精确POI B02520O0MV实时详情查询：4个已配置Key均返回10044 USER_DAILY_QUERY_OVER_LIMIT，当日额度不足，不能确认实时接口图片总数，不当作无图。
- 对历史缓存中该精确POI的3个原始图片URL逐一实时下载，全部HTTP200且可解码；尺寸见.runtime/attraction-gallery-batch/link-pilot/amap-B02520O0MV-live/report.json。此次没有猜高清参数，未做人工画面验收，3张可获取不等于3张质量合格或新增。
- 图片仅保留在独立审图缓存，未写content、未启动批采、正式仓未动。

## 2026-09-11｜灵湖景区：Trip站内搜索→详情页→高清图片完整短测成功

- 从Trip首页真实搜索框“目的地、景點、酒店等”输入简体“灵湖景区”，首条结果“靈湖景區 臨海, 台州, 浙江, 中國”；点击打开新标签，得到POI10520631精确详情页。没有使用外部搜索或跳过发现步骤直达已知链接。
- 对搜索获得的链接运行现有probe_trip_direct.js：两次独立HTTP200，POI/名称/城市通过，各提取1张；原始图片直接HTTP200下载，2500×1406、4473093字节。程序请求与下载共2.719秒（不包括浏览器搜索操作耗时）。尺寸/过暗检查通过，720px缩略图为湖面落日与小舟、无明显水印或海报。不能将网页960px展示图误判成源图分辨率不足。
- 本次证实发现瓶颈可通过Trip站内搜索解决单例，原来的外部搜索失败不等于Trip无页面/无图；尚未将浏览器站内搜索接入无人值守批量任务，也未声称全国稳定性已验证。
- 用户确认的采集原则：每源有几张合格图就保留几张，单源不足3/5张不记失败；3–5张是景点跨源累计目标。此源贡献1张成功，若当前无其他图，距最低3张还缺2张，应换源补缺，不反复采耗尽源。
- 证据及隔离候选：.runtime/attraction-gallery-batch/link-pilot/trip-10520631/{search-proof.json,direct-probe.json,quality.json,1.jpg,preview.jpg}；10+10清单已登记。仅更新试验产物与记录，未写content、未启动后台、未修改正式仓。

## 2026-09-11｜供链试验1续：Trip精确页通过直接程序采图验证

- 用户明确以可沉淀为无人值守批量采图的标准验证Trip链接（locale=zh-HK&curr=HKD，POI13412956）。本次没有打开浏览器、借用Cookie或调用完整图库API。
- 新增有界工具`scripts/probe_trip_direct.js`：仅beta运行，输入精确Trip链接、期望名称/城市；两次不使用本地页面缓存的独立HTTP请求，核对POI ID/名称/地域，提取页面已提供的图库URL，最多5张、下载并发2、每请求8秒超时，失败即记录，结果只写.runtime。不猜图片尺寸URL，不反复请求WAF接口。
- 实测约5.05秒：两次页面HTTP200、实体核验通过，各提取5张；5张直接下载全部200。实际尺寸2500×833、2500×1667、1667×2500、1667×2500、1600×1066；全部通过原尺寸、过暗、双感知哈希去重检查，小联系表初查是景观图且无明显水印/海报。未宣称已做逐原图人工验收或真实手机长期稳定性测试。
- 产物`.runtime/attraction-gallery-batch/link-pilot/trip-13412956/direct-probe.json`和`contact.jpg`，原始5张为隔离审图缓存；10+10清单已追加direct_program_trial_passed。
- 可复用经验：先发现并固定精确实体页→直接解析页面内图库→不足3张才考虑额外补源；本例完全不需要浏览器操作图库或请求full-gallery API。现有全量路径无条件继续请求额外API/发现源，会增加无必要耗时与错误，应在机制整合时改为分层按缺口触发。
- 结论：这条Trip精确页值得纳入程序批采适配；两次短测不是全站/全国长时稳定性证明，还要用其他景点链接验证泛化。保持后台暂停，未写content或同步正式仓。

## 2026-09-11｜供链试验1：Tripadvisor黄河石林成功取得3张候选

- 用户补充要求：本次及后续供链均快速判断能否得图，持续失败及时换链接，不反复耗时。采用短验证、一次必要短重试，先回复可用性，不为单链接开展大范围修复。
- 用户Tripadvisor链接实体d1851282，景泰黄河石林名称及白银景泰龙湾村地址吻合；注意不是Trip.com。真实浏览器首次设备检查自动结束后进入页面，无手动挑战操作；相册244张旅行者照片。直接Node请求同页HTTP403、777字节设备检查，不能把此响应当无图。
- 通过精确实体相册首图/下一张读取浏览器实际加载的dynamic-media-cdn.tripadvisor.com图片URL，保留页面给出的参数、不猜高清路径、不扫推荐景点。已下载3张1000×667/1000×750候选，HTTP200；其中一张首次TLS握手超时，单次短重试成功。三张压缩联系表复核为不同峡谷景观，无明显水印/海报或近似重复；这不是长期移动端稳定性证明。
- 结果与原始候选仅在`.runtime/attraction-gallery-batch/link-pilot/huanghe-tripadvisor/`，verification.json保留URL/尺寸/hash，contact.jpg为小联系表；第一组试验清单已登记成功。没有写入content或扩大来源白名单，后台保持暂停，正式仓不动。
- 可复用经验：来源分为可直接采集与需浏览器协助；设备检查要等待一次实际页面状态，不无限重试HTTP403；相册属旅行者上传，不能标成官方图，必须逐图筛选；本例可快速补候选，不意味着Tripadvisor已可无人值守批量采集。用户供链验证授权不自动等同图库发布授权。

## 2026-09-11｜用户提议10项供链验证＋10项独立发现

- 评估：合理。第一组用用户找到的链接隔离验证实体核验、图片区提取、高清获取、筛选与预览；第二组不依赖用户链接，单独验证发现能力，不能把第一组成功等同全国自动发现已解决。
- 固定清单`.runtime/attraction-gallery-batch/link-pilot-10-plus-10.json`：第一组黄河石林国家地质公园、绶溪公园、灵湖景区、天山大峡谷、邛海公园、文瀛公园、大若岩-陶公洞、惠州西湖红花湖景区、黄柏塬原生态风景区、渔梁坝；均来自当前待补池。前三项已有本次浏览器实查链接，可直接复用，用户不必重复查找，若有更好来源再补。
- 第二组由固定种子从其余待补项选定10项，保存ID、名称、地域与原有图片数，不根据结果临时换样本。尚未运行，等待第一组链接验证与机制修复。
- 评价分别记录找页成功、实体核验、图片获取、质量通过及最终补齐数量/耗时。最低3张维持，原有图片也须与新图去重；达到5张不强求。每页来源真实性、画面准确、尺寸、水印和加载仍须检查。10+10为小样本机制试验，不能据此承诺全国完成率。
- 只固定试验清单和记录，没有启动采集、改策略或写图库；后台保持paused，正式仓不动。

## 2026-09-11｜3个原失败景点的真实浏览器快速对照

- 用户要求只拿3个原失败景点快速验证浏览器→精确页面→图片链路。本次未重启后台、未改采集策略/图库，未同步正式仓；只记录发现与小规模请求对照。
- 黄河石林国家地质公园（amap_B03AF002D7，白银）：原selected=1，官网/Trip发现未命中，百科no_exact_page。浏览器搜索第一页找到Trip精确页`https://hk.trip.com/travel-guide/attraction/jingtai/yellow-river-stone-forest-national-geological-park-13412956/`，名称与甘肃白银景泰地址一致；点击首图打开标为“官方相片”的相册，显示37张（该标签不是官方授权证明）。DOM实际加载的两张图片为1200×800。将该精确URL交给现有Node parseTripAttractionGallery，不改解析器即HTTP200提取5张。已证明首要断点在页面发现，而不是该实体无图或解析器完全不能取图。
- 同一黄河石林维基条目由浏览器实际打开`https://zh.wikipedia.org/wiki/黄河石林`，正文明确完整景区名和白银市，并有景区配图；旧记录no_exact_page不等于没有条目。短标题/别名解析链路需要针对检查，本次未把标题差异直接认定为唯一根因。
- 绶溪公园（amap_B02520O0MV，莆田）：原selected=1。浏览器找到`https://hk.trip.com/travel-guide/attraction/putian/shouxi-park-22866907/`，名称/莆田地址匹配，相册显示1张；Node对该页也提取1张，说明页面发现可修，但此单一Trip相册当前不足3张。转到搜索已发现的福建日报精确报道`https://www.fjdaily.com/app/content/2021-06/23/content_1056428.html`，页面记者/通讯员署名文图、城市与景点一致；DOM实测5张图片尺寸为1080×1495、4390×2026、1268×713、4608×2128、1920×1080，另1张966×547不达标。这里是媒体报道，不冒称景区官网；5张尺寸合格不代表已完成逐图语义/水印/去重与最终验收。若正式纳入，需要明确来源类型和精确正文图片区范围。
- 灵湖景区（amap_B02400TOA1，台州）：原selected=0。浏览器找到`https://hk.trip.com/travel-guide/attraction/linhai/linghu-lake-scenic-area-10520631/`，地址浙江台州临海大道匹配；Node提取1张，浏览器主图960×539，不达现有门槛。找到`https://lhnews.zjol.com.cn/lhnews/system/2022/03/17/033533620.shtml`，正文说明文字由市文广旅体局提供、照片由灵湖景区及署名摄影者提供，已实际读取至少8张600×375照片；当前版本均尺寸不足，不猜原图URL、不降低要求。它是本地媒体上的景区供图文章，并非独立官网。下一步需真实原图入口或另一高清来源。
- 对照产物`.runtime/attraction-gallery-batch/browser-three-probe-20260911.json`记录3条浏览器确认URL对应的Node请求与解析结果：全部HTTP200、实体名正确、分别5/1/1张。只输出统计、少量图片URL/尺寸，无原图批量输出、无Base64。
- 结论：3/3找到精确Trip页面；黄河石林现有解析器在拿到正确链接后即可取得5张，绶溪有另一权威图文高清候选，灵湖仍卡在合格尺寸。不能宣称3/3图库验收完成。优先把真实浏览器页面发现和精确来源绑定接入，再针对媒体/景区供图补充来源规则；之后才扩大规模。

## 2026-09-11 09:26｜暂停低收益后台并分析未补齐原因

- 用户授权先暂停低效策略、分析原因。后台已在批次末安全paused，worker/supervisor锁均消失，断点完整；没有重新启动、修改采集策略、写图库或同步正式仓。
- 剩余2027项，按selected张数：0张571、1张916、2张540；全部标记retryable。统计基于每个景点最后保存的attempts，不是整夜累计请求；不同来源失败会重叠。详表`.runtime/attraction-gallery-batch/failure-analysis-20260911.json`。
- 主要瓶颈在来源发现与请求：官网发现2018/2027项含retryable_error，官网精确发现缓存仅8项；Trip精确发现缓存238项（有缓存不等于实体匹配及图库API通过）；剩余池没有已登记source-pages条目。官网发现平均约20.5秒、Trip发现约23秒，为各lane记录均值且并发，不能相加当单景点耗时。
- 携程1783项no_exact_page，244项曾返回候选；采集调用显式allowCityIndex:false，缺乏历史精确链接时不再发现城市列表。因此该来源大部分只复用旧绑定，不能把no_exact_page解释为携程无照片。
- 百科正文1281项retryable_error、680项no_exact_page；Wikimedia1356项retryable_error、474项no_exact_entity、186项缺城市/具体实体证据。同名广场公园等必须保留实体门禁，不能通过放宽城市核对换数量。
- 小样本实际请求“白银 黄河石林国家地质公园 官网”：Google HTTP200但现解析器取到0条；Bing HTTP200解析10条但目标名称匹配0；DDG HTTP202解析0条。这验证当前通用搜索发现链路的低命中，但单样本不代表全站；HTTP200不等于拿到可用搜索结果。结果`.runtime/attraction-gallery-batch/search-probe-20260911.json`，未输出或缓存完整网页到对话。
- 网络归因边界：历史错误只存error.message，大量fetch failed丢失cause/code/目标host，不能回溯断言是DNS、TLS、代理还是断网。当前Node24.15对Wikidata API与Google首页可HTTP200，不能据此否认昨晚请求失败。另验证4190端口被Node fetch按bad port拒绝，属于本机进度端口与fetch限制，不是公网源站失败根因；进度服务已用http健康检查，浏览器访问正常。
- 重试分类问题：搜索零命中、Trip名称/城市不匹配、网络临时故障均可能包装成retryable_error；Google不可用30分钟标记只在单个采集子进程内，8项一批重建进程使该发现失败记忆无法跨批复用。持久网络cooldowns仍有效，但不能替代搜索结果不可用缓存。全部2027项retryable让有限重试大量重复无新证据路径。
- 质量筛选不是当前最大阻点：现存rejected/visualRejected事件中，图片请求fetch failed324、尺寸/体积不足96、过暗16、近似重复7；这些是事件数，不是互斥景点数，不能与来源失败项数直接相减。
- 建议顺序：先补错误分类和host/cause证据，短暂故障才自动退避；搜索挑战/空壳与无实体分别留状态，无新证据不反复同页请求。再用540个只差1张池中的约20个跨原因代表项，验证站内精确实体发现、已有238个Trip缓存的详情/图库链路以及官网/百科定点来源，核对真实图片后评估成功率、耗时与额外请求，再决定扩展。暂不直接重新开启全国原策略、不降图质或实体门禁。

## 2026-09-11 09:20｜用户验收84项后写入beta

- 用户明确确认本轮新增候选OK，授权先写入beta，再商量下一步；未授权正式仓同步。
- 写入前固定`.runtime/attraction-gallery-batch/approved-20260911-84.json`：84个景点337张图片，逐项核对图片URL及顺序与用户所看隔离地图一致；没有纳入其他228个历史候选或后续新增候选。
- 实际新增83项、更新1项（遵义会议会址纪念馆），图库总数2854→2937。“84项新增候选”是本轮补源来源口径，不代表数据库84项都是新ID。
- 备份`.runtime/backups/attraction-gallery-overrides.20260911-091947.json`；变更`content/attraction-gallery-overrides.json`及构建生成data/dist。写入脚本针对审批子集显示pending=0，仅指审批快照，不代表全国待补已清零。
- 验证：npm run build成功；84项构建图片及封面逐项与审批快照一致，其他既有图库与备份逐项相同。仅刷新隔离索引，已写入84项退出待验收列表，现在本轮待审0、此前候选228；没有重启后台、修改采集策略或动正式仓。
- 下一步待商量：优先按失败层级统计剩余池，选择跨原因小样本验证精确来源发现和网络请求，再扩展有效策略；不要把网络空结果等同无图，不直接用原策略重复全国或降低质量门槛。此前228项仍未因本次批准而自动获得验收授权。

## 2026-09-11｜验收入口按本轮补源与历史候选分开

- 用户无法区分312项中的新增候选。已按后台启动时固定2111项ID清单分组：当前预览84项属于本轮补源候选，228项为此前积累候选，合计312；84项是9月10日下午启动以来的成果，不是仅午夜以后新增，也不是昨晚17:55以后净增22项。
- 后台预览`/preview.html`默认只列本轮84项，`/previous-review.html`列此前228项，`/all-review.html`保留全312项。页面说明数量合格不等于人工审图通过；历史候选也不能推断已验收。
- 修改`scripts/start_attraction_gallery_batch_preview.js`，以后后台每30分钟重建也保留分组。本次使用`--background --index-only`仅更新隔离索引，并逐项比对已运行地图快照的图片URL一致性，避免新清单指向旧图；未重启后台或预览服务，未写入图库和正式仓。
- 验证：脚本语法通过；生成结果84+228=312；HTTP200且默认页明确显示84项。验收每个景点的全部3–5张，检查实体一致、实景、清晰、无明显水印/海报/重复和大图加载。

## 2026-09-11 09:06｜过夜结果只读核对

- 重新确认cwd为beta，未授权同步正式仓。本次仅查看状态、日志末尾、进程与服务健康，不启动/重启、不写入候选内容。
- 09:06:01：原worker28580与supervisor22264持续运行，守护重启0次，心跳正常，phase=collecting；本池候选84/2111（约4.0%），实际处理过2106项，待补2027项，等待重试40项。相比昨晚17:55的62候选，过夜净增22项；不能把2106次覆盖理解为2106个已补齐。
- 现有隔离预览08:46:11更新，共312项，包含之前积累的未写入候选，不能称为昨晚新增312项。4190进度与4187预览健康检查均200。
- 最新阻点：精确来源页面发现未命中、实体/城市证据不足，以及百科请求失败、官网发现超时；采集程序持续运行但补齐收益低。没有达到剩余景点全部补齐目标；下一步应先验收已取得候选，再按来源发现/请求失败层级分析剩余项，不能把程序未命中归因为网上无图或继续承诺原策略能全补齐。

## 2026-09-10 17:55｜过夜前最终运行确认

- 已重新确认cwd为beta，未获正式仓同步授权；本轮仅只读核对和记录，没有重复启动或修改后台。
- 17:55:34：worker28580、supervisor22264仍为最终版本原进程，守护重启0次，status=running；候选待审62/2111，实际处理过596，仍待补2049。相比17:30的38候选、396处理项，持续有进展。
- 实际观察完整冷却接续：17:54在source_cooldown，上一批8项仅完成4项、只筛选4项；等待后自动采集下一批，17:55实际完成8/8并完成筛选和压缩，心跳持续更新。stop文件不存在，无需手动干预。
- 4190进度服务、4187预览服务健康检查200，预览页面200。明早先看实时页，再打开候选预览验收；若需要最新完整快照可运行preview_gallery_background.bat，避免在进度显示“正在更新隔离预览”时重复点击。
- 电脑须保持开机联网、不休眠。后台有限重试结束会保留可验收候选和明确未解决清单；现有证据支持持续运行，不支持保证明早全部2111项补齐或自动通过人工审图。

## 2026-09-10 17:30｜后台过夜可靠性修复与轻量实时进度

- 用户授权：直接分析、修复影响后台持续运行的问题，提供轻量可视化进度。全部操作在beta；未同步正式仓，未写入本批候选到content，未修改Gemini文档或前端业务交互、会话数据库。
- 实时入口：双击根目录`view_gallery_progress.bat`，只启动/复用只读进度服务，不启动或重启采集。电脑`http://127.0.0.1:4190/`，同局域网手机`http://192.168.16.43:4190/`；端口以`.runtime/attraction-gallery-batch/progress-server.json`为准。页面使用本地Vue 3，每5秒取约2–3KB摘要，页面隐藏时停止轮询，不加载图片、完整JSON或日志；显示候选待审、实际处理过、待补、重试、阶段、心跳、进程状态、当前景点和来源阻点。预览链接指向既有4187候选快照，与实时计数明确区分。
- BAT：新增`view_gallery_progress.bat`，更新`run_gallery_background.bat`为启动守护后打开进度页；均ASCII+CRLF，有路径/依赖检查、实际健康检查、本机及LAN地址和pause。`stop_gallery_background.bat`仍为当前批次后安全停下；停机重启后需手动启动BAT，未安装系统启动任务、未更改电源策略。
- 计数修复：采集器每组实际保存的结果携带`backgroundReceipt`，与主state同次原子提交；调度器只按匹配回执增加访问/尝试，崩溃接续重复回执不重复计数。未执行项不扣次数，源站临时失败独立计数与退避。旧调度文件先备份为`codex-background-before-v2-*.json`，迁移只保留更新时间支持的实际处理记录，36项旧误记/不确定计数恢复为未执行；不重跑已达标项。
- 调度：先覆盖未执行项，再有限重试；有效完整尝试最多2次，临时失败最多8次，退避2分钟逐步到1小时；未完成子进程失败单独最多5次，不冒充有效采集。遵守采集器resumeAfter和来源Retry-After，不再把超过10分钟的源站冷却截短。失败仍待补，最多尝试耗尽后明确`pass_complete_with_unresolved`，不是全部图片完成。
- 恢复：独立supervisor拉起worker，worker每5秒心跳；异常退出按30秒递增至5分钟恢复，心跳停滞10分钟清理自有进程树后恢复。每阶段有时限，超时等待进程树真正结束后才进入下阶段；崩溃遗留子进程核对父PID和脚本身份，最多等至该阶段启动5分钟后清理，无法核对则显式等待，不冒险并发写入。双锁防重复启动。
- 筛选失败从已保存候选恢复，不重新联网；连续3次筛选失败保留该项并让其他景点继续。预览或压缩失败留记录，不终止后续采集。后台不再清空已有人工审图联系表。审图压缩仍仅触及.runtime副本，无正式图片清理。
- 实际复现并修复Windows文件共享问题：读状态文件时偶发EPERM导致原子rename失败。新增JS/Python断点替换辅助函数，短暂占用最多重试约2秒；保留原子替换，不截断旧完整文件，永久失败仍明确报错。采集、筛选、压缩和调度状态均接入。
- 验证：实际安全暂停并接续；受控终止无采集子进程的等待中worker，supervisor约30秒后恢复且断点/候选保留；实际多批采集→筛选→压缩→保存继续推进。调度测试覆盖部分执行、幂等恢复、冷却、公平顺序、有限重试和排除项；子进程测试覆盖正常/非零退出、命令缺失、超时清理；Windows共享占用重试、网络缓存/Retry-After、来源解析回归通过；Python语法与BAT编码通过；npm run build成功。浏览器实际打开进度页、刷新和布局检查通过，API HTTP200；没有声称真实手机已验收。
- 最终部署快照17:30:04：supervisor PID22264、worker PID28580，running/collecting；目标2111项，候选待审38，实际处理过396，仍待补2073，等待重试27。这是本次剩余池口径；既有content图库仍2854项。新版本运行前恢复5项待机器筛选候选，并持续取得新候选；不得把38项视为人工审图通过。
- 必须继承：可以容忍限流、临时网络失败和工作进程退出，但无法保证源站放行或所有实体都能找到足量合格图片；断电、休眠、人工结束整个守护进程、永久磁盘/权限错误也不是自动修复能保证解决的情况。距明早仍有时间持续处理，不能承诺全部补齐。下一步先看实时页与具体阻点，验收已获得的候选；不要重启旧Gemini巡检/写入入口，不要把旧attempts数当真实有效完成数。
- 核心文件：`scripts/codex_gallery_background.js`、`gallery_background_policy.js`、`gallery_background_process.js`、`gallery_checkpoint_io.js/.py`、`gallery_progress_server.js`、`gallery-progress.html`；采集器、筛选器、审图压缩器、gallery_network及对应测试；package.json/package-lock.json新增Vue。运行锁、备份、状态、日志都留.runtime，不同步。

## 2026-09-10 16:43｜明早完成预估与计数风险

- 系统时间16:42:11，距2026-09-11 09:00约16小时18分钟；此时ready=23/2111，attempts记录232项，errors为空。该计数仅表示调度器登记，不可称为232项均已完整采集。
- 新日志已出现源站限流、采集器提前保存断点，明确提示未启动项保持原状；后台`scripts/codex_gallery_background.js:82`却对整批8项统一增加attempts，存在未实际执行项消耗最多两次尝试额度的风险。errors为空不能证明无来源失败。本次只读发现，未修改运行脚本或重启任务。
- 时间判断：队列遍历有可能在明早前结束，但目前无法可靠估算完整有效遍历时间，更不能承诺剩余景点全部补成3–5张合格图；程序候选仍需人工审图。若用户要求继续优化，优先修复限流等待与实际执行计数，再评估速度，不能靠空转消耗重试额度宣布跑完。
- 自查入口：直接打开4187的`/preview.html`看最近一次候选快照；查看`codex-background.json`的status/ready/total/updatedAt和日志末尾看实时运行。`preview_gallery_background.bat`会重新生成预览，并非纯状态查看；`inspect_gallery_health.bat`调用inspect_and_heal脚本，并非本后台只读进度入口，不建议作为查看进度按钮。

## 2026-09-10 16:40｜新对话接续与后台只读核对

- 已重新确认工作目录：所有后续工作仅在 `D:\github\lvyoumap-universal-serverbeta`；用户仍采用 beta 验收后手动运行 `sync_to_formal_git.bat` 同步的流程，本次未获正式仓修改授权。
- 16:39:48 快照：`codex-background.json` 为 running，PID 29204 与锁文件、实际后台主进程一致；已记录尝试184项、共184次，任务池2,111项中23项达到程序候选门槛，errors为空。检查期间ready从22增至23，日志和断点持续推进；尝试数、候选数均不等于人工验收完成数。
- 进程核对：观察到一个 Codex 图库后台主进程及其采集子进程，未发现第二个图库采集主进程；另有旧 `watch_xhs_progress.js` 进度显示进程，未作启停。未重复启动、重启或停止任何任务，未运行快照审计/写入脚本。
- 现有预览服务4185（上一批17项）和4187（后台候选）均通过 `/api/health` 与 `/preview.html` HTTP 200检查。后台预览为16:27生成的230项快照，与实时剩余池候选数口径不同；后台按既有30分钟周期更新，无需为本次接续重建。局域网地址沿用预览状态中的192.168.16.43，未作真实手机验收。
- 已梳理代码入口：原生HTML/CSS/JavaScript与ECharts地图（`index.html`、`style.css`、`app.js`）；`content`覆盖层经`scripts/generate_static_data.js`生成数据，`scripts/build.js`构建静态产物；`server/index.js`提供静态预览、健康检查和天气API。图库后台负责采集→机械筛选→审图缓存压缩→隔离预览，不自动写入content。
- 上次已交付beta的17项/60图及图库总数2,854沿用16:28记录，本次没有重新审图或内容写入。仅更新本交接文档，没有执行构建、同步、提交或部署。
- 对话控制：JSON只取必要统计、日志只读末尾，必要时才展示少量缩略图/压缩联系表。审图缓存与正式资源分开，清理前核对引用，不删除线上依赖图片；不修改会话数据库、Gemini文档或其前端改动。

## 2026-09-10 16:28｜Gemini下午成果审计、17项安全交付、全国静默续跑（交接入口）

- 用户最新授权：审计稳定图库后写入 **beta** 供自行同步；剩余全国图库后台跑，明天验收。没有授权修改正式 Git。不要修改 Gemini 文档，也不要覆盖其前端改动。
- 审计基线：state共5,171项；原固定待补池2,338项中243项处于可预览，5项待机器筛选，2,090项待来源。下午预览里“3,016”是全国累计数，不能当成剩余池完成数。机器标记 ready_for_user_review 只是候选达到数量/技术门槛，并非真实人工验收。
- 确认根因：①内容守卫仅查caption/title/URL/比例，没有识别画面；②Trip仍混用recommendPhoto，不能宣称官方认证/100%实景；③清水岩误匹配清水岩温泉度假酒店，原来名称包含匹配和缓存cityVerified放行过宽；④乾隆行宫通过Wikimedia博物馆大分类混入墓葬；⑤已登记黑名单的丽江植物园4张图仍残留selected。
- 本轮交付范围：250项有变化候选做结构审计，248项仍达到3张；从中分布抽查30项的全部图片，逐图筛选18项。63张浏览器无Referer实测62张成功，新疆人民剧场1张百科图加载失败后不足3张，暂缓。因此最终**17个景点、60张实际加载成功的图片**写入`content/attraction-gallery-overrides.json`，新增17、更新0、图库总数2,854；原2,837项保持不变。不能把未抽查的其余候选宣称已审计完成。
- 17项：长白山国家级自然保护区、泉湖公园、慕士塔格冰川公园、森兰绿地、直贡梯寺、独山子大峡谷、惠山古镇景区、三圣岛、成吉思汗陵园、关帝庙、1915广场、青莲寺、北京鲁迅博物馆、抚顺战犯管理所、九龙洞、安徽大青山野生动物世界、凤凰山森林公园。
- 先隔离预览后写入；自动备份`.runtime/backups/attraction-gallery-overrides.20260910-162516.json`。本批准确清单和选图在`.runtime/attraction-gallery-batch/codex-approved.json`；结构问题`codex-audit.json`；浏览器结果`codex-browser-check.json`。拒绝清单新增URL检查与既有图库无重叠。
- 核心修复：Trip图库API只取officialPhoto（名称不等于可证明官方授权）；保留图片原始title；即使缓存有cityVerified也重新检查Trip详情实体，名称改为规范化等值匹配，避免“清水岩”包含匹配酒店；Wikimedia分类图不再仅凭分类归属放行；采集逐图剔除，3–5张保留，不整组清空好图。仍需明天看画面，不能承诺程序已消除所有无关图。
- 后台入口：`run_gallery_background.bat`（静默启动/断点续跑），`preview_gallery_background.bat`（重新生成并显示电脑/手机预览地址），`stop_gallery_background.bat`（本批结束后安全停下）。三个BAT均ASCII+CRLF，启动校验依赖。运行时电脑须开机联网、不休眠；重启后手动再次点启动BAT。
- 后台实现：`scripts/codex_gallery_background.js`，单写入锁、8项一批/4并发、采集阶段4分钟超时、单批失败继续其他批，先首轮覆盖再最多二次尝试、优先只差1张项；不在20%停下、不自动写入内容。每30分钟和结束时生成隔离预览。最终若仍未补齐，状态为pass_complete_with_unresolved并保留具体来源失败记录，不能虚报全部完成。与旧Gemini后台/巡检不要同时运行。
- 后台范围：启动时全部pending_sources/ready_for_visual_review共2,111项；原有可预览但未人工验收项仍在隔离预览。真实实时进度见`.runtime/attraction-gallery-batch/codex-background.json`，日志`codex-background.log`；锁`codex-background.lock`。结束对话不会主动停止任务。
- 预览：本批17项使用`.runtime/previews/attraction-gallery-batch/state.json`记录地址（本次4185）；后台使用`.runtime/previews/gallery-background/state.json`（从4187选端口）。以对应state.json的实际端口为准。服务启动后检查/api/health。索引都为`/preview.html`，手机使用局域网IP192.168.16.43（换网络后重新点预览BAT）。
- 空间：审图缓存曾约19GB。新增`codex_compact_gallery_cache.py`只压缩.runtime中审图副本，保留线上URL、原尺寸、原hash，另记reviewHash；每批最多80张或10秒，禁止全量压缩拖住采集。初始全量压缩尝试超时，已终止旧子进程并以有界实现重启；不删除正式图片。联系表仅6张小图，不把全量图片送进对话。
- 验证：来源解析和网页来源回归通过；BAT编码检查通过；构建成功；隔离索引17项；浏览器地图搜索及详情打开、60张外链图实际加载检查。后台已实际经过多批采集、筛选、断点保存、暂停和恢复；新增候选数量以实时状态为准，不代表全部质量通过。
- 新对话最短接续提示：**请先读docs/CODEX_MARK.md最新记录，以及.runtime/attraction-gallery-batch/codex-background.json和日志末尾。继续全国图库验收，先汇报实际进度；不要重跑已完成项，不动正式仓，不修改GEMINI_MARK.md。** 不必附带旧长对话。脚本codex_gallery_audit/finalize/prepare为本次快照操作，不要在后台写入时重复执行。

## 2026-09-10｜全国剩余图库：精确页发现提速、真实进度与未解决阻点

- 当前边界：本阶段只在 beta 的 `.runtime/attraction-gallery-batch` 生成候选和断点，未把本阶段新候选写入 `content/attraction-gallery-overrides.json`，未修改正式 Git。固定剩余清单为 2,338 个，第一轮 20% 验收目标 468 个；截至本次交接，109 个已完成视觉筛选并可供用户复核，9 个停在待视觉筛选，2,220 个仍待补源。不能把全局状态中的累计完成数或尝试数当成本轮完成数。
- 已验证的有效提速：Trip 站内搜索使用隐藏浏览器并发读取真实渲染结果，从每轮 4 个提升到 8 个；24 个搜索可在约 8-12 秒内完成。`gallery_discovery_bridge.js` 的发现版本为 3，只保存名称命中且城市中文或拼音命中的页面，写入 `cityVerified: true`；旧版未带城市核验的页面不能直接信任。
- 已接入 Trip 完整图库接口：`POST https://www.trip.com/restapi/soa2/19912/getTripPoiPhotoGallery`，新增 POST JSON 缓存、`trip-page`/`trip-gallery` 独立限流和 `parseTripPhotoGallery()`。接口未被拦截时能返回大量 1080-2700px 原图；例如黔灵山已验证有效。
- 当前核心阻点不是“网上没图”：连续请求后 Trip 接口返回 HTTP 430，响应头明确为 `WafAntibotCheckFailed`。此前代码声称 `fallback_to_page` 但没有真正抓详情页，已修成接口失败时实际解析公开详情页；然而多数详情页首屏只嵌入 1 张低分辨率头图，仍不足以补齐 3 张，不能把这种结果归因为景点缺图。
- 已修复重复耗时：`run_fresh_trip_galleries.js` 仅消费 `discoveredAt > item.updatedAt` 的新发现且城市已核验页面，自动调用快速 Trip 采集、视觉筛选并更新固定里程碑；同一失败页面不会在无新来源时反复执行。`fast-secondary` 只跑已发现的 Trip 精确页，慢源留给确实不足的项目。
- 已修复多页面漏采：同一景点最多两个城市核验页面会聚合图片，第一页面为空时继续第二页面，拿够 5 张即停。实际重跑表明部分页面仍只有低清头图，说明这项修复正确但不能绕过 WAF。
- 百科快速通道结论：固定清单有 346 个待补项命中现有百科标题索引，其中 99 个只差 1 张；试跑 72 个仅 2 个达到数量门槛，已及时停止。标题命中不代表正文有合格大图，不应原样重复这轮低收益任务。
- 严格实体规则：POI 数字 ID 是 Trip 实体的权威部分，URL slug 可能被忽略。测试时猜测 `...-76857` 实际跳转到大连“秀月峰景区”，证明禁止猜 URL、猜 POI ID或仅凭 slug 采图；必须使用站内搜索返回的精确链接并核对页面名称和城市。
- 下一步优先级：先研究浏览器已登录/已通过 WAF 的 Trip 详情页“查看全部”图库和页面资源，验证能否批量取得原图链接；若不能，再对剩余项并行发现景区官网精确图库和百科正文图库。不要继续无差别调用已被 430 的 Node 接口，也不要重复跑已证明低命中的百科标题清单。
- 本轮验证：`gallery_source_parsers`、`gallery_web_sources` 测试通过，相关脚本语法检查通过。未达到 468 个，尚未生成本轮 20% 地图原样隔离预览，不能宣称本阶段已交付。
- 关键文件：`scripts/gallery_network.js`、`scripts/gallery_source_parsers.js`、`scripts/gallery_web_sources.js`、`scripts/gallery_source_policy.js`、`scripts/gallery_discovery_bridge.js`、`scripts/run_fresh_trip_galleries.js`、`scripts/run_gallery_remaining.js`、`scripts/collect_attraction_galleries_batch.js`、`package.json`、`package-lock.json`。运行态断点位于 `.runtime/attraction-gallery-batch/`，不应同步到正式仓或线上。

---

## 2026-09-09｜剩余图库按20%验收：并行取源与断点修复（进行中）

- 用户目标：剩余景点全部补成3–5张真实清晰图，每完成20%给隔离预览；不能用错图、低清图凑数，不能把请求失败当成网上没有图片。固定剩余范围2,338个，首轮需真正完成468个，不能以尝试次数代替完成数。未获本批验收前不写入图库内容，更不修改正式Git。
- 官网、Trip独立发现、百科正文、既有携程绑定并行获取；搜索引擎仅用于发现页面。Google浏览器搜索有效，但Node请求只返回脚本外壳；Bing曾返回完全无关泛化结果，DDG遇到202挑战页及403。发现失败必须保留具体错误，不能宣称源站缺图。
- 新增请求去重、成功JSON缓存、按来源限速和Retry-After冷却。修复百科一家限流连带暂停其他百科的问题；暂停批次只记录实际执行项，恢复时校正旧版误记的未执行项。`remaining-milestones.json`固定任务分母，完成20%后停止等待验收。
- 发现并修复实质漏图：百科解析先删除信息框再校验城市，恰好把仅在信息框里的地域证据删掉，导致有图页面被错拒。现先校验地域再移除信息框等非正文图片区，乔家大院实际恢复至5张候选。
- 为减少逐景点搜索，新增每次最多50个标题的百科精确解析索引；复用百科自身重定向和繁简转换，后续优先处理已发现页面。正文图已有原始尺寸和标准缩略图URL时直接复用，避免再逐条调用图片元数据API；最终仍检测实际图片尺寸、去重和来源。
- Google浏览器找到茶卡天空壹号的Trip精确页（POI 56290165），已登记，可提取5张候选；经现有视觉筛选保留5张。乔家大院与其共用一张小联系表检查，不为全量任务生成截图。
- 测试：请求缓存/冷却测试、搜索解析和信息框地域回归通过；2个定点实例已实际采集和渲染。全国20%尚未完成，不能据此宣布全量可验收。新增结果仅在.runtime候选中。
- 关键文件：`scripts/gallery_web_sources.js`、`scripts/gallery_network.js`、`scripts/run_gallery_remaining.js`、`scripts/seed_gallery_wikipedia.js`、采集/渲染/预览脚本、`content/attraction-gallery-source-pages.json`。Gemini历史不改。

## 2026-09-09｜第二次用户补源验证：纠正“困难项”归因

- 教训：用户连续两次从官网、Trip、百科找到丰富实景图，证明此前把采集未命中笼统称为“困难项”不准确。应首先排查页面发现、实体绑定、网络请求、解析、域名和质量筛选，不能以程序返回空数组推断网上缺图，也不能反复要求用户逐条找链接。
- 已确认根因：南京数据库名称仅为“总统府”，Wikidata 首个同名结果是通用概念；旧白名单漏掉实际 API 返回的高清缩略图域名 `thumb.wikimedia.org`；官网仅有3个手工登记实体；百科异常原先静默返回空数组，未记录请求失败。
- 本次修复：百科先查询城市限定名称并保留原名称查询；接纳 Wikimedia 官方缩略图域名；记录百科命中实体、候选数量或请求错误；官网解析提前剔除 JS/CSS/SVG 和 logo2 等标志文件；来源策略升级为2，已完成图库不重跑。
- 实测：南山海上观音官网取得3张尺寸合格候选，南京总统府19张、天津古文化街20张；仅对这3个执行视觉去重后分别保留3、5、5张，生成一张约165KB联系表。数量合格不等于图片构图已最优，南京样本含石狮、地面等局部细节，后续选图应优先景观全貌并保留差异视角。
- 本轮采集曾误带虞山国家森林公园ID，已核对其仍为待补0张，仅本地采集断点更新，未写入地图内容；随后按正确ID补跑天津古文化街。定点任务必须校对ID对应名称再执行。
- 当前边界：本次3个仅进入候选隔离验收，未写入内容。仍未实现全国官网自动发现、Trip独立发现和维基百科正文图库解析；不能把上述设想描述为已完成，也不能凭3个样本宣称全国全部图片已覆盖。
- 后续必须继承：复用已有内容中的精确官网/Trip/百科链接与已确认实体绑定；每个来源独立尝试，Trip不能以携程先返回照片为前置条件；达到3张即停止昂贵补源。仅对未完成项按失败层级成批修复，记录具体原因、尝试来源、下一步动作。禁止原策略无差别重复全量跑、无限重试或用错图低清图凑数。
- 验证：来源策略和解析现有测试通过；定点视觉脚本增加 `--ids`，只处理指定项并准确报告范围。隔离预览入口使用相对链接，手机点击不再跳到手机自己的127.0.0.1。
- 涉及：`scripts/gallery_source_policy.js`、`scripts/collect_attraction_galleries_batch.js`、`scripts/gallery_source_parsers.js`、`scripts/render_attraction_gallery_batch.py`、`scripts/start_attraction_gallery_batch_preview.js`、`content/attraction-gallery-source-pages.json`。Gemini文档和正式Git未修改。

## 2026-09-09｜全国搜索相关度修复

- 问题/目标：短关键词会匹配介绍和地址，且结果未按相关度排序，例如搜索“王府”出现苏州博物馆、温都水城等弱相关项。
- 核心改动：搜索仅保留景点名称、精确城市和特色标签；按名称完全一致、名称开头、名称包含、城市、标签依次排序，同级按评分排序，最多展示20条。
- 涉及文件：`app.js`。
- 验证结果：构建成功；“王府”由20条泛匹配收敛为12条名称或标签相关结果，正文和地址误命中项已排除。
- 必须继承：介绍和地址只用于详情展示，不再作为普通短词搜索入口；扩展搜索需通过明确的分词或高级搜索设计实现。

---

## 2026-09-09｜全国图库来源收敛与全量隔离预览

- 用户验收后已将第二批1,693个新增或变化图库写入 beta；原有1,123个结果保持不变，`content/attraction-gallery-overrides.json` 当前共2,837个，并保留写入前备份 `.runtime/backups/attraction-gallery-overrides.20260909-110328.json`。正式 Git 未修改。
- 修复构建层级冲突：旧 `image-overrides.json` 只作为单图基础层，图库覆盖最后统一决定 `image`、`image_source` 和 `images`，避免封面被旧单图重新覆盖。构建与图库批次检查通过，2,816个已完成景点均为3-5张唯一图片、封面一致、无拒绝图及通用搜索图。
- 用户确认首个全国50%批次后，将708个新增图库写入 beta；原有415个结果未变化，`content/attraction-gallery-overrides.json` 当前合计1,144个，并生成 `.runtime/backups/attraction-gallery-overrides.20260909-095738.json` 作为写入前备份。构建成功，正式 Git 未修改。
- 新增唯一来源策略 `scripts/gallery_source_policy.js`：官网、高德、Trip、携程、百科的优先级、稳定域名、对外来源名称和重跑条件统一定义；采集、隔离预览、写入共同引用。来源策略带版本号，旧困难项在策略升级后自动补跑一次，成功或已完整尝试的来源不再无意义重复请求。
- 修复旧城市索引最多30页串行扫描导致全量任务预计耗时数小时的问题：每个城市只建立一次共享索引，限定携程前10页并以5页并发获取；百科精确实体查询限定两个已确认名称。第一次48条慢任务停止后从断点恢复，未丢失结果。
- 全国5,192个景点已完成一轮全量来源处理：其中5,171个进入统一断点，另21个已有图库保持不重跑。精确补源后2,833个达到基础数量门槛；视觉去重、暗图和拒绝清单筛选后，2,816个最终达到3-5张，2,354个保持待补，1个非景点排除。
- 相对当时已写入的1,144个图库，本轮共有1,693个新增或变化图库进入地图原样隔离预览并完成用户验收，随后已写入 beta。
- 视觉检查默认只固定随机抽取30个景点、生成5张联系表（约1.46MB），不再为全量结果生成数百张临时图片；完整候选仍在隔离地图中逐景点查看。当前 Codex 活动任务日志约152MB，未再次出现GB级图片膨胀。

涉及：`content/attraction-gallery-overrides.json`、`scripts/gallery_source_policy.js`、`scripts/test_gallery_source_policy.js`、`scripts/collect_attraction_galleries_batch.js`、`scripts/render_attraction_gallery_batch.py`、`scripts/start_attraction_gallery_batch_preview.js`、`scripts/apply_attraction_gallery_batch.js`、`package.json`、`.runtime/attraction-gallery-batch`（仅本地断点和隔离预览）。

---

## 2026-09-08｜累计 50% 图库二次随机审查

- 对本轮720个新增或变化图库使用固定随机种子抽取360个（50%），共复测1,164张图片；全部外链二次请求成功，实际尺寸继续满足长边至少1000、短边至少560，未发现失效链接或源站降清。
- 人工逐页复核30张联系表，覆盖图片是否属于对应实体、是否为实景、是否有明显水印或重复；明确剔除16张导览图、活动/营销海报、宣传缩略图及无关生活照。
- 问题 URL 写入图库拒绝清单，后续全国批次不会再次选用；剔除后不足3张的景点自动退回待补，不以低质量图片补位。审查只作用于 beta 候选和规则，正式 Git 未修改。
- 二次审查后的最终状态为：1,123个图库达到3-5张，708个新增或变化图库进入隔离预览，1,472个不足3张的困难项保留断点；本轮不再扩大抽查范围。
- 新增可复用的 `scripts/audit_attraction_gallery_sample.py`，固定比例抽样、在线复测和联系表生成均只写入 `.runtime`，审查结束后联系表可直接清理，不进入同步或部署。

涉及：`scripts/audit_attraction_gallery_sample.py`、`content/attraction-gallery-image-denylist.json`、`.runtime/attraction-gallery-audit`（仅本地审查产物）。

---

## 2026-09-08｜全国图库累计 50% 批次隔离验收

- 全国 5,192 个景点中累计建立 2,596 个图库处理断点，达到本轮“累计跑到 50%”目标；不重跑已写入图库，不修改正式 Git。
- 第一层批量复用现有主图、景区官网登记页、文旅部名录、高德精确 POI/子景点；第二层只复用已经精确绑定的 Trip、携程与公开百科实体来源，未使用通用搜索引擎或模糊同名图片。
- 为避免城市级 OTA 扫描和百科逐项查询拖慢全国任务，新增 `--fast-secondary` 快速补源模式：并发 12、Trip 精确请求并发 4、达到 3 张即停止追图；不足 3 张保留断点，不用低清、水印或无关图片凑数。完整困难项补源仍可后续按断点单独执行。
- 自动清晰度、文件体积、暗图、拒绝清单和感知去重后，2,596 个中 1,135 个达到 3-5 张；相对 beta 已写入的 436 个图库，本轮共有 720 个新增或变化图库进入统一隔离预览，1,460 个困难项保留待补，1 个非景点保持排除。
- 隔离预览运行于 `http://127.0.0.1:4185/preview.html`，仅叠加候选数据，尚未写入 `content/attraction-gallery-overrides.json`；必须等待用户验收确认后才能应用。

涉及：`scripts/collect_attraction_galleries_batch.js`、`.runtime/attraction-gallery-batch`（仅本地断点和隔离预览）。

---

## 2026-09-08｜景区官网与 Trip 精确图库纳入全局来源层

- 根因修正：此前图库白名单主要依赖高德、携程和公开百科，错误遗漏了景区官网与 Trip 精确实体图库；大量“待补”实际是来源发现能力不足，不代表互联网没有真实图片。
- 新增 `content/attraction-gallery-source-pages.json` 作为困难项精确来源登记表。景区官网只采集登记页面、指定图片区段和同域路径；Trip 只解析精确 POI 或页面内精确同名图集，禁止扫描评论、附近景点、酒店或通用搜索结果。
- 携程结构化详情中的全局 `poiId` 可直接绑定 Trip 同一实体图库；采集器会自动构造 Trip 精确实体页补图，不再要求逐景点人工搜索 Trip 链接。
- Trip 页面缓存增加结构校验：缺少 `__NEXT_DATA__` 的风控/残页不会缓存复用；站点并发限制为2，并对网络失败短重试，解决“网页有图但批处理并发时被误判待补”。
- 定点 `--ids` 补源不再改写全国批次规模；已将误显示为100的批次状态无联网恢复为实际520条，后续可在原批次上直接扩容且不会重跑已完成项。
- 三个真实样本已通过清晰度、连通性、暗图和感知去重：厦门园林植物园官网选出5张园区实景；黄果树旅游景区由高德+Trip选出5张；香港中环摩天轮由高德+Trip选出5张。
- 同一机制额外自动救回中山公园、杨柳青古镇；太原植物园新增候选因画面过暗被视觉门禁退回，没有为数量放行。固定520条批次从109个待补降至104个，415个达到3-5张。当前结果只在 beta 断点与隔离验收状态中，尚未写入图库，正式 Git 未修改。
- 用户完成隔离预览验收后，已将本轮100个新增或变化图库安全写入 beta；原有336个保留，`attraction-gallery-overrides.json` 当前共436个。构建成功，图库批次415/415条均为3-5张唯一图片、封面一致，未命中拒绝清单或通用搜索图；剩余104个困难项继续保留断点，正式 Git 仍未修改。

涉及：`content/attraction-gallery-source-pages.json`、`scripts/gallery_source_parsers.js`、`scripts/test_gallery_source_parsers.js`、`scripts/collect_attraction_galleries_batch.js`、`scripts/render_attraction_gallery_batch.py`、`scripts/start_attraction_gallery_batch_preview.js`、`scripts/apply_attraction_gallery_batch.js`。

---

## 2026-09-08｜图库困难项全局补源与城市级实体索引

- 复用 `attraction-overrides.json`、`manual-attractions*.json`、核心清单和历史运行证据中的携程详情页，按景点 ID、省份、城市、规范名称与已确认别名建立一次性索引；同名实体不跨城市匹配。
- 新增城市级携程索引：同一城市的待补目标合并扫描一次，最多30页，找到全部目标立即停止；页面与详情请求共享压缩缓存，单条失败只保留断点，不阻断整批。
- 支持已确认别名和自动去城市前缀别名，例如“上海世纪公园”仅在上海城市索引中匹配“世纪公园”；携程详情页仍二次校验实体名称，避免跨城市同名误配。
- 修复稳定 CDN 返回 `application/octet-stream` 时被提前误判的问题：先读取二进制，再以真实图片尺寸、文件体积和比例作为质量判定；同时明确排除高德 `/sns/ugccomment/` 用户评论图片。
- 公开百科改为信任已严格匹配的 Wikidata 实体主图与其 Wikimedia Commons 专属分类；分类内仍执行许可、清晰度、暗图、噪声类型和感知重复图检查，不做通用图片搜索。
- 204个困难项完成两轮断点补源：基础数量合格415个，经感知去重后全批520个中410个达到3-5张；相对已写入图库，本轮新增或变化95个，剩余109个继续保持待补，1个非景点保持排除。
- 本轮95个待验收图库共420张：高德精确POI 155张、携程精确实体252张、公开百科精确实体13张；全部3-5张、URL唯一、未命中拒绝清单、无高德评论图。
- 本轮只更新 beta 采集机制、本地断点和隔离预览，尚未写入 `attraction-gallery-overrides.json`，未修改正式 Git。

涉及：`scripts/collect_attraction_galleries_batch.js`、`scripts/gallery_source_parsers.js`、`scripts/test_gallery_source_parsers.js`、`.runtime/attraction-gallery-batch`（仅本地断点）。

---

## 2026-09-08｜全国图库10%批次与高效率分层方案

- 建立固定的跨省10%图库批次，共520个景点；批次 ID 持久化到断点状态，后续写入或重跑不会导致样本漂移，也不会重跑已完成项。
- 采集拆为两层：第一层只跑现有图、文旅名录和高德精确 POI；达到3张立即结束。第二层只处理不足3张者，再调用携程精确实体相册和公开百科。本轮两层均一次完成，无死循环和整批阻断。
- 第一层新增处理420个景点后，连同原样本共218个通过视觉门槛；第二层仅处理302个缺口，最终315/520达到3-5张，通过率60.6%。其中52个已在 beta，本轮新增263个进入隔离预览，尚未写入内容。
- 合格图片来源统计：高德精确POI 859张、携程精确实体相册278张、高德子景点40张、公开百科8张。未使用通用图片搜索。
- 剩余204个困难项中，36个为0张、64个为1张、104个为2张；192个主要因为携程省级索引未定位到精确实体页，说明下一轮的重点应是“省市+别名+结构化ID”的实体索引，而不是降低图片质量。
- 新增非景点自动排除与重复请求保护：本轮识别并排除“索溪山寨·湘西民间土菜(标志门店)”；已完整执行第二层但仍不足3张的项目默认不再反复联网，只有显式要求重试才运行。
- `run_gallery_batch.bat` 已固化两层流程，默认运行520个固定样本；以后传入更大的全国规模即可扩展同一断点，不再另写临时脚本。BAT保持ASCII命令与CRLF换行。
- 当前隔离预览只展示相对已写入数据新增的263个景点。运行缓存已压缩至约113MB；图片仍为外链，未写入正式图片目录。
- 本轮未写入这263个图库，未修改正式 Git；等待用户抽查后再决定写入范围。

### 验收后写入

- 用户完成抽查后，已将本轮263个图库写入 beta；原有73个保留，当前 `attraction-gallery-overrides.json` 合计336个景点图库。
- 构建成功；本批315/315个景点均为3-5张唯一图片，封面与首图一致，未命中拒绝清单，也未混入通用搜索图片。
- 204个不足3张的困难项和1个非景点排除项均未写入；正式 Git 尚未修改，等待用户运行同步工具。

涉及：`scripts/collect_attraction_galleries_batch.js`、`scripts/render_attraction_gallery_batch.py`、`content/attraction-gallery-policy.json`、`package.json`、`.runtime/attraction-gallery-batch`（仅本地断点）。

---

## 2026-09-08｜图库全局门槛调整为3-5张

- 新增唯一图库策略文件 `content/attraction-gallery-policy.json`：最低3张、目标5张、最多5张；质量与实体准确优先，不为补足第4或第5张使用错图、低清图、水印图或不稳定来源。
- 采集、视觉筛选、隔离预览、写入和构建后校验统一读取该策略，消除过去多处写死“必须5张”造成的流程不一致；以后调整数量只改策略文件。
- 备用来源仅在不足3张时继续触发；已有3-4张合格图直接进入验收，减少无意义联网请求和等待。
- 隔离预览只展示相对已写入图库新增或变化的项目，不要求重复验收已发布图库；写入脚本支持新增、更新、跳过未变化项，并继续执行原子写入和自动备份。
- 现有100个样本按新规则重新归类：78个达到3-5张，其中52个已经写入 beta，本轮新增26个待验收；真正不足3张的剩余22个继续保留断点。

涉及：`content/attraction-gallery-policy.json`、`scripts/collect_attraction_galleries_batch.js`、`scripts/render_attraction_gallery_batch.py`、`scripts/start_attraction_gallery_batch_preview.js`、`scripts/apply_attraction_gallery_batch.js`、`scripts/verify_attraction_gallery_batch.js`、两份旧试点脚本。

---

## 2026-09-08｜首批52个景点图库写入 beta

- 用户完成隔离预览后，将本批52个景点图库写入 `content/attraction-gallery-overrides.json`；原有21个图库完整保留，当前合计73个景点图库。
- 只写入状态为 `ready_for_user_review` 且恰好5张的候选；其余48个来源不足项继续保留在运行断点中，没有混入正式内容。
- 每个新图库把已验收首图同步设为景点封面，避免前端自动追加旧封面后出现6张；生成器仅对图库层开放 `image`、`image_source`、`images` 三个字段，不影响其他景点资料。
- 新增原子写入、自动备份与构建后校验脚本。构建通过，52/52 均为5张唯一图片，封面一致，无拒绝清单图片及通用搜索图片。
- 本次只写入 beta，未修改正式 Git；等待用户运行 `sync_to_formal_git.bat` 后自行提交和部署。

涉及：`content/attraction-gallery-overrides.json`、`scripts/apply_attraction_gallery_batch.js`、`scripts/verify_attraction_gallery_batch.js`、`scripts/generate_static_data.js`、`package.json`、生成后的 `data`。

- 同步工具同时移除了“全国报告非阻断项”和“验收后存在新改动”两类无实际操作价值的警告；真正的路径、文件完整性、JavaScript语法、超大文件等安全检查继续保留。

---

## 2026-09-07｜隔离预览按钮失效根因修复

- 现象：图库地图隔离预览中，PC端“查看大图”和详情右上角关闭按钮均无响应。
- 根因：构建及预览脚本使用全局 `app.js` 字符串替换缓存版本号，误命中 HTML 内“由 app.js 动态生成”等注释，并吞掉直到下一处引号之间的HTML，造成DOM结构损坏和事件初始化中断。
- 全局修复：`scripts/build.js` 只允许在 `src="app.js"`、`src="china_geo.js"`、`href="style.css"` 属性内替换版本；图库预览脚本同样只替换脚本 `src`，以后页面正文和注释出现文件名也不会被破坏。
- 已重新原子构建 beta 并重建52个景点隔离预览。PC自动化验证：大图打开、大图关闭、详情关闭全部成功；手机390×844触屏环境验证：切换至2/5、大图打开和关闭成功；浏览器运行错误为0。
- 本次只修复构建/预览机制，没有写入图库候选，没有修改正式 Git。

涉及：`scripts/build.js`、`scripts/start_attraction_gallery_batch_preview.js`。

---

## 2026-09-07｜52个图库地图原样隔离预览与缓存防膨胀

### 隔离预览

- 新增 `preview_gallery_batch.bat` 与 `scripts/start_attraction_gallery_batch_preview.js`，把首批52个五图候选叠加到独立地图站点；不修改 `content`、`data`、正式图库或正式 Git。
- 预览复用当前 `dist/assets` 和 `dist/vendor` 的目录链接，仅复制约46MB的页面与数据，不再复制约1.5GB静态资源。
- 每个预览景点强制使用本批选中的5张图，并将首张同步设为封面，避免“原封面+5张候选”变成6张。
- 当前入口：`http://127.0.0.1:4185/preview.html`；同一局域网可用 `http://192.168.16.43:4185/preview.html`（局域网地址以后可能变化）。

### 图片补全机制结论

- 框架已经形成：精确实体绑定、稳定来源白名单、尺寸/体积/比例/移动UA硬门禁、哈希与感知去重、拒绝清单、联系表视觉初审、地图原样隔离预览、人工确认后才写入。
- 机制底层闭环已具备，但全国来源覆盖仍未健全：首批100个中52个达到5张，48个缺稳定来源；外链长期可用性和逐图语义仍需地图/真机验收，不能把 `ready_for_user_review` 当作已发布。
- 旧21个图库中的18张通用搜索图和6张高德评论图属于历史债务，必须按新稳定来源规则复查。

### 防止空间和会话再次膨胀

- 原空间根因是审图原图约896MB和未压缩页面缓存约366MB，不是9张联系表。
- 页面缓存改为 gzip，保留7天并设置500文件/200MB硬上限；审图原图在联系表生成后压缩为720×540以内的复核缓存，保留原始URL、原始尺寸和原始哈希，不降低地图实际外链清晰度。
- 本次图库运行目录由约1269.76MB降至82.50MB，其中复核图32.50MB、页面缓存42.69MB；断点状态和联系表保留。
- 后续不再把整批联系表或大量原图嵌入对话，只提供本地预览链接并抽查少量目标；关键结论写入本文档，避免会话上下文因图片重复进入GB级。

涉及：`scripts/start_attraction_gallery_batch_preview.js`、`preview_gallery_batch.bat`、`scripts/compact_attraction_gallery_runtime.py`、`scripts/collect_attraction_galleries_batch.js`、`run_gallery_batch.bat`、`package.json`。

---

## 2026-09-07｜图库断点恢复、稳定来源提速与实图初审

### 当前结果与边界

- 仅在 beta 工作，未修改正式 Git、未写入地图图库、未部署；Gemini 文档和前端修改未动。
- 当前仍是跨省 **100 个样本**，不是全国完成。达到 5 张候选图的景点由 20 个增加至 **52 个**，另 **48 个**保留断点。候选暂存 `.runtime/attraction-gallery-batch/state.json`，9 张联系表在该目录的 `contact-sheets` 下。
- 已浏览全部联系表，并新增拒绝 11 个 URL，涉及海报、叠加水印/宣传标识、拼贴与疑似异地图片。拒绝清单累计 12 项。长隆、香港迪士尼、德天等不能为凑数放行。
- `ready_for_user_review` 只表示有 5 张可供进一步验收的候选，不代表逐图原尺寸、真实手机外链加载及实体真实性全部通过。联系表初审不能代替最终地图验收；不得直接全量写入。

### 根因与修复

- 携程旧逻辑泛抓 `imageUrl`、截前两张，容易混入非相册内容。改为解析 `__NEXT_DATA__ → poiDetail → imageInfo.poiPhotoImageList`，核对实体名称，记录页面、POI 和图片 ID；不扫评论、相关推荐图片。
- 省份列表的 `?keyword=` 未形成有效筛选。改为最多 3 页实际列表，复用历史携程/二次补证链接；同页并发请求合并、HTML 缓存 7 天，避免各景点反复下载同一页。
- 备用源触发改为按已通过尺寸检查的图片数，不再按原始链接数量。只对未完成景点执行，有少量候选余量供去重，不重跑已完成图库。
- BAT 与 npm 入口加入 `--repair-pending`；改变批量大小不清空状态，`--max-items` 可限定本轮工作量；按旧断点优先续跑，单项异常不终止整批。
- 状态采用临时文件原子替换；审图文件改用内容哈希命名，恢复时校验哈希，避免旧索引文件被覆盖后显示错图。保留成功的高德缓存；此前未成功取得高德数据的记录允许以后重试。
- 修正 dHash 行索引；保留已筛选结果，记录近似重复/过暗/解码失败原因；命中拒绝清单会重新筛选。
- BAT 保持 ASCII + CRLF，增加路径、脚本及 Python 图片依赖预检，错误保留窗口。

### 验证与后续

- `node scripts/test_gallery_source_parsers.js` 通过：正确实体、错误实体拒绝、仅相册提取、缺失结构拒绝，以及缓存的灵隐寺真实页面 4 张相册图。
- 采集脚本语法检查通过；实际完成两轮仅缺口续补、渲染与实图初审。审图缓存不属于正式部署资源，图片仍保留外链。
- Android User-Agent 下载检查仅代表本机 HTTP 连通，**不等于真实手机/微信可用性验证**。自动尺寸检查也不等于保证清晰、无水印；高德 POI 图片同样可能有海报，不能称为官方审核实景图。
- 下一步只补这 48 个缺口，解决实体页面索引/别名与照片质量问题，再扩大全国；禁止改用搜索引擎或降低质量凑 5 张。
- 抽查候选：白帝城·瞿塘峡景区、万岁山武侠城、南浔古镇、云冈石窟、黄鹤楼。检查 5 张是否均为对应景点、无明显水印和海报、原尺寸清晰；地图及真实手机验收尚未执行。

涉及：`scripts/collect_attraction_galleries_batch.js`、`scripts/gallery_source_parsers.js`、`scripts/test_gallery_source_parsers.js`、`scripts/render_attraction_gallery_batch.py`、`content/attraction-gallery-image-denylist.json`、`run_gallery_batch.bat`、`package.json`。

---

## 2026-09-04｜全国景点图库稳定来源批处理

### 关键改动

- 新增独立批处理 `run_gallery_batch.bat`，负责候选采集、高清升级、移动端连通检查、清晰度检查、文件哈希与双感知哈希去重、联系表生成；支持断点续跑，不接入总控。
- 新增高德接口本地缓存，重复验证不再反复消耗 Key 配额。
- 仅使用稳定来源：现有已验证图、文旅部官方名录、高德正式 POI/准确子景点、携程精确景点详情页前两张、Wikidata/Wikimedia 精确实体；明确禁止百度/Bing 等通用图片搜索。
- 高德 `store.is.autonavi.com/showpic` 统一使用 `type=7` 高清档；每个精确 POI 最多取接口返回的三张正式图片，带 `_AIGC` 标记及 `aos-comment` 用户评论图片全局排除，避免海报、截图和无关人物混入。
- “5 张”是写入门槛，不是降低质量凑数的依据；不足 5 张的景点保持待补来源，不写入图库。
- 新增持久图片拒绝清单；视觉复核发现的水印图、错图可按 URL 全局禁用，之后重跑不会再次混入。
- 首轮跨省 100 个样本经自动硬门禁与视觉去重后，20 个达到 5 张，80 个保持待补。结果只存在 `.runtime/attraction-gallery-batch` 隔离区，尚未写入正式图库数据。

### 涉及文件

- `scripts/collect_attraction_galleries_batch.js`
- `scripts/render_attraction_gallery_batch.py`
- `content/attraction-gallery-image-denylist.json`
- `run_gallery_batch.bat`
- `package.json`

### 后续必须继承

- 全国任务采用“批处理完成机械采集和硬门禁，联系表完成最终视觉复核”的组合，不让批处理自行猜测图片语义。
- 只有通过视觉复核的景点才能进入 `content/attraction-gallery-overrides.json`；任何写入和同步正式 Git 仍需用户确认。

---

## 2026-09-03｜景点图库试点与移动端大图性能

### 关键改动

- 为 21 个试点景点建立多图图库，每个景点保留 5 张经筛选图片。
- 建立图片语义、连通性、分辨率、水印和移动端可用性检查；宁可替换低质图，不保留错图、截图或水印图。
- 高德 `store.is.autonavi.com/showpic` 使用 `?type=7` 高清档位；禁止误改 `aos-comment` 原始文件流水号。
- 上海迪士尼、乌镇、张家界、大唐西市等试点完成错图、水印、海外低可用图源纠偏。
- PC 大图按真实比例展示，避免低分辨率图片强行撑满屏幕。
- 大图关闭改为 `pointerdown` 即时响应；关闭后释放图片 `src`，隐藏状态不再重复解码全尺寸图片。
- 大图关闭增加点击穿透保护，避免连带关闭详情弹窗或重新打开大图。

### 涉及文件

- `app.js`
- `index.html`
- `style.css`
- `content/attraction-gallery-overrides.json`
- `scripts/collect_attraction_galleries.js`
- `scripts/write_gallery_sources.js`

### 验证

- 21 个图库、105 张图片完成 Android UA 连通检查，失败 0。
- `node --check app.js` 与 `npm run build` 通过。
- 本地实际交互测试：关闭大图约 0.3 秒，详情弹窗保持打开。

---

## 2026-09-02｜第二阶段内容完善完成

### 关键改动

- `content-remaining` 共 1,769 条：1,692 条景点介绍、77 条懒人攻略全部采集完成。
- 从历史原始回答直接回收 65 条完整简介，避免重复采集。
- 点点结果增加规范化名称匹配，不再要求逐字完全一致。
- 采集器采用 5 条、3 条、单条递减批量，并增加页面自愈和 150 秒无进度看门狗。
- 内容完善阶段门槛调整为：简介不少于 25 字且不是旧占位模板；不再用过高字数门槛误拦完整内容。
- 修复 6 条历史乱码实体，采用按 ID 定点覆盖，不重写原始大数据库。

### 验证

- 全批次采集剩余量：`RemainingIntro=0`、`RemainingLazy=0`。
- 隔离预览按“景点介绍/懒人攻略”原页面样式完成验收后写入 beta。

---

## 2026-09-01｜第二阶段内容完善首批

- 首批 `content-01` 共 197 条完成定向修复、隔离预览、用户验收并写入 beta。
- 仅完善异常字段，不覆盖已经正确的基本资料、旅行指南和懒人攻略。
- 预览必须明确列出抽查景点、抽查字段和判断标准，避免只给笼统名单。

---

## 2026-08-31｜第一阶段优先修复完成

- 建立全国景点内容快速扫描，区分“优先修复”和“内容完善”。
- 第一阶段优先修复异常介绍、明显模板、实体错配和错误标签；空懒人攻略不属于该阶段阻断项。
- 第一阶段采用约 10% 节点抽查，通过后继续全量；最终完成并经隔离预览验收。
- 修复 `<br>` 被作为普通文字展示的问题，同时保留合法换行结构。

---

## 2026-08-28｜景点内容分层与修复边界

### 三层内容边界

- 基本信息：景点介绍、开放时间、门票、地址、电话、游玩时长、季节和特色标签。
- 旅行指南：路线、交通、住宿、美食和出行建议。
- 懒人攻略：点点生成的结构化实用攻略，不得用旅行指南模板冒充。

### 必须继承

- 高德优先负责实体、地址、坐标、电话、评分和官方图片。
- 景区官方与公开百科用于事实补证。
- 点点负责更适合人阅读的攻略内容，但不得覆盖已确认的事实字段。
- 修复前必须先判断错误属于哪一层，禁止把一种内容写入另一层。

---

## 2026-08-27｜景点基本信息与特色标签

- 全国景点基本信息聚焦四项核心字段：开放时间、门票参考、具体地址、联系电话。
- 按收费景区、免费景区、开放式地点区分必填逻辑：开放公园、广场、街区不强制虚构电话或固定营业时间。
- 点点批量补证采用每批 3–5 个景点，结果写入前先生成隔离预览。
- 特色标签固定为每个景点 3 个；A级景区优先展示等级，其余标签强调真实文化、自然或体验特色。
- 禁止“国家级、热门、其他”等低价值占位标签泛滥。

---

## 2026-08-26｜全国关键信息盘点规则

- 缺失清单按景点经营属性分类，不再把所有景点套用同一字段模板。
- 免费开放型重点补地址和开放属性；收费经营型重点补门票、开放时间、地址和电话。
- 资料采集可综合高德、景区官方、文旅部门、公开百科及主流旅游信息，不设置“证据不足待核实”作为偷懒归类。
- 所有新数据必须保留字段来源和采集时间，写入前提供可读预览。

---

## 2026-08-25｜全国美食列表数据流程

### 当前结构

- 美食数据指省市景点列表旁的美食推荐卡片，不是景点弹窗内的美食段落。
- 总控保持 3 个入口：`补全`、`预览确认`、`写入数据`。
- 全国任务一次运行，按省、市归属整理；无法精确到市的才进入“其他”。

### 数据来源与边界

- 名称和地域：存量旅行指南、地方公开资料及城市归属统计。
- 图片：优先稳定公开图源并进行名称、实物、尺寸和连通检查。
- 描述与标签：优先复用真实存量与公开事实；禁止模板套话冒充采集结果。
- 评分：只有明确公开评分来源时才展示，禁止按提及次数伪造平台评分。
- 写入前必须跨地区、跨品类抽测；个别失败保留断点，不阻断全国批次。

---

## 维护格式

后续追加记录使用以下精简格式：

```markdown
## YYYY-MM-DD｜改动主题

- 问题/目标：
- 核心改动：
- 涉及文件：
- 验证结果：
- 必须继承：
```

## 2026-09-16｜修正盱眙象山景点乱码

- 问题/目标：待处理清单显示 `盱眙象山国��矿山公园`，用户确认景点有价值，改正名称而非删除。
- 核心改动：将同一 ID `amap_B0FFJV2J1R` 的乱码名称统一修正为 `盱眙象山国家矿山公园`。
- 涉及文件：当前图库草稿、图库源状态、`content/db.json`、江苏省数据及展示索引。
- 验证结果：相关 JSON 可读取；`4210` 接口返回修正后的名称，草稿 revision 41、270 条保持不变；旧乱码已从活动数据清除。
- 必须继承：保留该景点及现有图片/历史记录；只改 beta，正式仓不动。

## 2026-09-15｜Trip 旅游度假区简称搜索补全

- 问题/目标：`江郎山省级旅游度假区` 未继续用简称 `江郎山` 搜索，错误进入手填。
- 核心改动：发现策略升级至 v6；补充国家级、省级、国际及普通旅游度假区/旅游区后缀，并优先剥离最长完整后缀。
- 涉及文件：`scripts/gallery_trip_discovery.js`、`scripts/test_gallery_retry_policy.js`。
- 验证结果：生成查询词 `江郎山省级旅游度假区`、`江郎山`；Trip 候选 `江郎山风景名胜区（衢州）` 自动命中；工作台仍为单实例 `http://127.0.0.1:4210/`，草稿 revision 33，用户已填 URL 未丢失。
- 必须继承：用户手填链接优先保留；策略词表更新需提升版本使旧发现缓存失效；只改 beta，未改正式仓。

## 2026-09-16｜待补剩余 13 条来源拆分

- 问题/目标：生成 270 条清单后仍显示 13 条可生成，且全部被误标为网络异常。
- 核心改动：确认生成前池内实际为 283 条；本次取 270 条后剩余 13 条。修正生成池分类和页面提示，分别显示未尝试、早期未填链接回流、网络失败和规则升级重试。
- 涉及文件：`scripts/gallery_link_batch_actions.js`、`scripts/gallery_link_batch_server.js`、`scripts/gallery_link_batch_ui.js`、`scripts/gallery-link-batch.html`。
- 验证结果：剩余 13 条中 12 条为早期未填链接回流，1 条（衡水湖旅游景区）为网络失败；当前草稿 revision 41、270 条未修改；4210 单实例已重启，相关工作台测试通过。
- 必须继承：剩余计数代表当前草稿之外仍可生成的项目；生成指定条数不会自动吞并超出数量的项目。

## 2026-09-16｜全国景点图库补全收口盘点

- 问题/目标：确认最后 270 条及后续收尾批次完成后，图库是否已经全部处理完。
- 核心结论：待补生成池为 0；无采集或写入进程运行；最新 270 条批次与最后 30 条批次均完成并已写入 beta。
- 最新写入：270 条批次写入 211 个景点、467 张图；30 条收尾批次写入 3 个景点、9 张图。两个未写入的旧批次均为 0 个候选，无待验收图片。
- 有效图库分布：5,169 个有效景点中 0 张为 0；1 张 594 个；2 张 791 个；3–5 张 3,289 个；超过 5 张 495 个。
- 验收口径：按已确认的“小众景点允许保留一张稳定图”标准，本轮补全已收口。部分项目没有新增图，但均保留至少一张现有有效图并在写入时标记暂不再补。
- 必须继承：上述数字来自 beta 当前有效图片合并结果；正式仓尚未因此次盘点被修改。

## 2026-09-16｜工作台新增图片审查与人工删除

- 问题/目标：补图收口后快速发现错图、无关图，并允许用户在现有网页勾选确认删除。
- 核心改动：`4210` 工作台新增“04 图片审查”；支持高风险、本轮采集、全部图片三种分页列表及搜索。高风险按跨景点共图、明确子景点来源、宣传/导览标题和本地文件缺失排序。
- 删除保护：只有本机页面可提交；浏览器二次确认；每个景点至少保留 1 张；删除只移除 beta 引用并重建，不直接删除共享/线上文件；人工移除的景点-图片关系会记录，后续批次不能重新写回。
- 涉及文件：`scripts/gallery_image_review.js`、`scripts/gallery_link_batch_server.js`、`scripts/gallery_link_batch_ui.js`、`scripts/gallery-link-batch.html`、`scripts/gallery_link_batch_apply.js`、`scripts/test_gallery_link_workbench.js`。
- 验证结果：当前共 18,295 张图片；高风险 210 张（5 页，每页 48）；本轮采集图 10,474 张；最后一张保护 1,067 张。接口、页面视觉、删除回滚与防回写测试通过；4210 仅一个服务实例，无采集任务。
- 必须继承：只改 beta；正式仓未修改。审查删除产生的 `content/attraction-gallery-review-decisions.json` 后续同步时属于正式内容数据。
- 共图说明补强：审查卡片现显示同图关联的其他景点，勾选文案改为“仅从此景点移除”；提交仍以 `景点 ID + 图片 URL` 为删除单位，未勾选景点的同图引用不受影响，图片实体不会物理删除。若某景点会被删至 0 张，整次提交会拒绝并回滚。相关语法、工作台回归测试和在线接口检查均通过，服务单实例 PID `23016`。
- 审查页增加“全选本页可删除项”和“取消本页”；全选会按景点计算已选数量并自动跳过最后一张保护项，仍需点击顶部删除按钮并二次确认后才写入。
- 发布前复核：用户人工审查后共落盘 107 条 `景点 ID + 图片 URL` 移除关系；Beta 完整构建通过（34 省级数据、搜索索引与 `dist` 正常）。同步脚本预览通过，预计同步 34 个文件、约 85.02 MB，包含审查决策、图库覆盖、生成数据及工作台代码，不包含 `.runtime`、`dist`、`reports`、`.env`、日志或临时文件；正式 Git 检查时为干净状态，尚未执行复制。
