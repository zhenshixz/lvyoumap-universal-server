# 当前接续摘要（2026-09-15）

- 只在 `D:/github/lvyoumap-universal-serverbeta` 开发、采集、验收；正式仓未授权不动。
- 工作台：http://127.0.0.1:4210/#form；LAN http://192.168.16.43:4210/#form（只读）。
- 清单页在生成条数旁轻量显示剩余可生成量；当前为1668条。已移除100条人为上限，输入超过剩余量时生成全部；请求体支持千级清单保存。
- 最新批次 `20260914-160643-44ab79` 已暂停等待切换 IP：断点59/100，触发项旗山万佛寺，后台无worker；普通续跑已验证不会绕过暂停。
- 规则：已有图片只能追加；自动搜索同地域首项；地域别名归一；名称保守模糊/别名搜索；Trip 明确占位图/图库 0 标记无图并关闭补录；图片无尺寸限制，仍检查协议、大小、解码、过暗、重复。
- “采集已填及已自动选链接”会同时使用手填URL和已确定的自动候选；已成功访问但图片全部不合格的同一URL会明确显示历史原因并禁止无效重跑，换新链接才重试。
- 自动发现策略版本5增加通用名称后缀变体搜索；例如“贵阳百花湖风景名胜区”会尝试“贵阳百花湖/百花湖”，地域仍需匹配贵阳，并记录实际查询词。
- HTTP 432 为 Trip WhaleGuard 风控；现规则为首次432立即暂停整批，不请求后续景点、不在同一IP自动重试。切换IP后在进度页点击“我已切换 IP，继续运行”，从断点继续；成功项不重复。旧的“快速重跑”按钮仅适用于已完成批次的临时失败项。
- 不输出完整日志、JSON、原图；只报告摘要、数量、错误和路径。
